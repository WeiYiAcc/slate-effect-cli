#+TITLE: SEC Project Tasks
#+TODO: TODO(t) PLANNED(p) IN_PROGRESS(i) | DONE(d) CANCELLED(c)
#+STARTUP: overview
#+OPTIONS: toc:3 num:t

* SEC Project                                                      :sec:cli:effect:

Effect-native AI CLI using Effect Agent framework.

** DONE sec-core
   简化的核心 CLI 功能
   - run, chat, session, models 命令

** DONE sec-run
   sec run 单次调用
   :PROPERTIES:
   :END:
   - Provider: CLIProxyAPI (openrouter/openrouter/free)
   - 测试: "1+1等于多少" → "1+1等于2。"

** DONE sec-chat
   sec chat REPL 会话
   :PROPERTIES:
   :END:
   - 多轮对话
   - 自动保存 session

** DONE sec-session
   sec session 持久化
   :PROPERTIES:
   :END:
   - 路径: ~/.local/share/sec/sessions/{id}.json
   - 命令: new, list, show, rm

** TODO sec-serve
   sec serve HTTP server
   :PROPERTIES:
   :END:
   - /v1/chat/completions endpoint
   - 用于 opencode TUI

** TODO sec-acp
   sec acp ACP 协议
   :PROPERTIES:
   :END:
   - for multica spawn/serve

** DONE sec-effect-agent
   effect-agent 重构
   :PROPERTIES:
   :END:
   - 从直接 fetch 迁移到 effect-agent runtime
   - 关键: Schema-first + Agent.make + Tool.make

** DONE sec-agent-schema
   Schema-first 定义
   :PROPERTIES:
   :END:
   - UserMessage, AiResponse, ReadParams
   - 用 Schema.Class

** DONE sec-agent-runtime
   Runtime 集成
   :PROPERTIES:
   :END:
   - AgentRuntime.run
   - Provider: OpenAiClient + OpenAiLanguageModel

** PLANNED sec-tools
   工具调用系统
   :PROPERTIES:
   :END:

** PLANNED sec-tool-read
   读取文件
   :PROPERTIES:
   :END:

** PLANNED sec-tool-bash
   执行 bash
   :PROPERTIES:
   :END:

** PLANNED sec-tool-edit
   编辑文件
   :PROPERTIES:
   :END:
   :DEPENDS: sec-tool-read

** PLANNED sec-tool-websearch
   网页搜索
   :PROPERTIES:
   :END:

** PLANNED sec-agent-tools
   Toolkit 组合
   :PROPERTIES:
   :END:

** PLANNED sec-tui
   TUI 界面
   :PROPERTIES:
   :END:
   - 参考 pi-tui
   :DEPENDS: sec-tools

** PLANNED sec-multica
   multica 集成
   :PROPERTIES:
   :END:
   - multica shim
   :DEPENDS: sec-acp

** IN_PROGRESS sec-docs
   文档
   :PROPERTIES:
   :END:

** DONE sec-docs-usage
   使用文档
   :PROPERTIES:
   :END:

** TODO sec-docs-api
   API 文档
   :PROPERTIES:
   :END:

** TODO sec-docs-arch
   架构文档
   :PROPERTIES:
   :END:

* Progress Log

** 2024-09-04
   - [X] jj-forklift 安装成功
   - [X] slate-effect-cli 转换为 jj colocate 仓库
   - [X] 推送到 GitHub: https://github.com/WeiYiAcc/slate-effect-cli
   - [X] 创建 PR #1 并合并
   - [X] 用 effect-agent 模式重写 sec
   - [X] sec run 测试通过

* Sync Locations
:PROPERTIES:
:END:
任务数据被持久化到多个位置：
- [[file:.sec-tasks/tasks-dag.json][tasks-dag.json]] (JSON)
- [[file:.sec-tasks/tasks.md][tasks.md]] (Markdown)
- [[file:.sec-tasks/tasks.org][tasks.org]] (Org-mode)
- [[file:.sec-tasks/tasks.edn][tasks.edn]] (EDN for Clojure)
- ariadne-fact (structured fact DB)
- logseq graph (block-based notes)
- prime-agent ai-memory (durable memory)
