#!/usr/bin/env node
import { AgentSideConnection, type NewSessionRequest, type PromptRequest, type PromptResponse } from "@agentclientprotocol/sdk";
import { type PermissionMode } from "@anthropic-ai/claude-agent-sdk";
type ClaudeSdkRuntime = Awaited<typeof claudeSdkRuntimePromise>;
type QueryFactory = ClaudeSdkRuntime["query"];
declare const claudeSdkRuntimePromise: Promise<{
    cliPath: string;
    query: typeof import("@anthropic-ai/claude-agent-sdk").query;
}>;
export declare class ClaudeQuerySession {
    private readonly conn;
    readonly sessionId: string;
    private readonly cwd;
    private mode;
    private readonly pathToClaudeCodeExecutable;
    private promptQueue;
    private query;
    private readyPromise;
    private pendingTurn;
    private lastEmit;
    private pendingMcpServers;
    private mcpServersApplied;
    private activeToolCalls;
    private toolUseBlockIndex;
    private reader;
    private closed;
    private cancelled;
    constructor(conn: AgentSideConnection, sessionId: string, cwd: string, mode: PermissionMode, params: NewSessionRequest, pathToClaudeCodeExecutable: string, queryFactory: QueryFactory);
    get currentMode(): PermissionMode;
    ready(): Promise<void>;
    prompt(params: PromptRequest): Promise<PromptResponse>;
    cancel(): Promise<void>;
    setMode(mode: PermissionMode): Promise<void>;
    close(): void;
    private initialize;
    private applyPendingMcpServers;
    private consume;
    private handleMessage;
    private handleStreamEvent;
    private handleAssistantMessage;
    private handleToolProgress;
    private handleSystemMessage;
    private handleResult;
    private emitText;
    private emit;
    private emitToolCallUpdate;
    private createPermissionHandler;
    private findToolCallByIndex;
}
export {};
