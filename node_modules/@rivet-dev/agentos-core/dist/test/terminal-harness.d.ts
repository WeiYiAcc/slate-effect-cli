/**
 * TerminalHarness wires openShell() to a headless xterm Terminal so tests can
 * assert against deterministic terminal screen state.
 */
import { Terminal } from "@xterm/headless";
import type { Kernel } from "../runtime-compat.js";
type ShellHandle = ReturnType<Kernel["openShell"]>;
export declare class TerminalHarness {
    readonly term: Terminal;
    readonly shell: ShellHandle;
    private typing;
    private disposed;
    constructor(kernel: Kernel, options?: {
        cols?: number;
        rows?: number;
        env?: Record<string, string>;
        cwd?: string;
    });
    type(input: string): Promise<void>;
    private typeInternal;
    screenshotTrimmed(): string;
    line(row: number): string;
    waitFor(text: string, occurrence?: number, timeoutMs?: number): Promise<void>;
    exit(): Promise<number>;
    dispose(): Promise<void>;
}
export {};
