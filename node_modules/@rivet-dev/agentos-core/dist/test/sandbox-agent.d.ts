import type { SandboxAgent } from "sandbox-agent";
export interface MockSandboxAgentHandle {
    baseUrl: string;
    client: SandboxAgent;
    path(...segments: string[]): string;
    rootDir: string;
    stop(): Promise<void>;
}
export declare function startMockSandboxAgent(): Promise<MockSandboxAgentHandle>;
