import { SidecarProcess, type SidecarSpawnOptions } from "./native-process-client.js";
export interface AgentOsSidecarProcessHandle {
    client: SidecarProcess;
    dispose(): Promise<void>;
}
export declare function spawnAgentOsSidecar(options: SidecarSpawnOptions): AgentOsSidecarProcessHandle;
