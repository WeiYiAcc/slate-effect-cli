import { SidecarProcess, } from "./native-process-client.js";
export function spawnAgentOsSidecar(options) {
    const client = SidecarProcess.spawn(options);
    return {
        client,
        dispose: () => client.dispose(),
    };
}
