import { Cron } from "croner";
export type ParsedSchedule = {
    kind: "date";
    date: Date;
} | {
    kind: "cron";
    cron: Cron;
};
export declare class InvalidScheduleError extends Error {
    readonly schedule: string;
    constructor(schedule: string);
}
export declare class PastScheduleError extends Error {
    readonly schedule: string;
    constructor(schedule: string);
}
export declare function parseSchedule(schedule: string): ParsedSchedule;
export declare function resolveSchedule(schedule: string, now?: Date): {
    parsed: ParsedSchedule;
    nextRun?: Date;
};
export declare function validateScheduleForRegistration(schedule: string, now?: Date): {
    parsed: ParsedSchedule;
    nextRun?: Date;
};
