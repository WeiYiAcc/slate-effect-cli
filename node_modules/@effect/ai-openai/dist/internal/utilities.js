const finishReasonMap = {
  content_filter: "content-filter",
  function_call: "tool-calls",
  length: "length",
  stop: "stop",
  tool_calls: "tool-calls"
};
/** @internal */
export const escapeJSONDelta = delta => JSON.stringify(delta).slice(1, -1);
/** @internal */
export const resolveFinishReason = (finishReason, hasToolCalls) => {
  if (finishReason == null) {
    return hasToolCalls ? "tool-calls" : "stop";
  }
  const reason = Object.hasOwn(finishReasonMap, finishReason) ? finishReasonMap[finishReason] : undefined;
  if (reason == null) {
    return hasToolCalls ? "tool-calls" : "unknown";
  }
  return reason;
};
//# sourceMappingURL=utilities.js.map