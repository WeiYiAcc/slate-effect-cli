import { t as __exportAll } from "./rolldown-runtime-D7D4PA-g.mjs";
import { a as choosePruneBound, f as renderForSummary, i as buildCompactedView, l as estimatePromptTokens, n as COMPACTION_INSTRUCTION, o as chooseSummarizeCut, s as collectCoveredMessages } from "./compaction-B-u2WNrm.mjs";
import "@effect-agent/core/AgentPolicy";
import { Context, Effect, Layer, Schema, Stream } from "effect";
import { Prompt } from "effect/unstable/ai";
//#region src/ContextCompactor.ts
var ContextCompactor_exports = /* @__PURE__ */ __exportAll({
	CompactionDecision: () => CompactionDecision,
	CompactionError: () => CompactionError,
	ContextCompactor: () => ContextCompactor
});
/** A proposed view change. Source indices are exclusive prefix bounds, never record sequences. */
const CompactionDecision = Schema.Union([Schema.Struct({
	kind: Schema.Literal("clear-tool-results"),
	through: Schema.Natural
}), Schema.Struct({
	kind: Schema.Literal("summarize"),
	through: Schema.Natural,
	summary: Schema.NonEmptyString.check(Schema.isMaxLength(8388608), Schema.isPattern(/\S/))
})]);
/** Expected strategy or decision-validation failure. Causes stay in the live Effect only. */
var CompactionError = class extends Schema.TaggedError()("CompactionError", {
	message: Schema.String.check(Schema.isMaxLength(4096)),
	cause: Schema.optionalKey(Schema.Defect())
}) {};
const defaultCompactor = (model) => ({
	estimate: estimatePromptTokens,
	compact: (request) => Stream.suspend(() => {
		const { source, policy, targetTokens, forceSummarize, allowSummarize } = request;
		const state = { ...request.state };
		const keepRecentTokens = targetTokens === void 0 ? policy.keepRecentTokens : Math.max(1, Math.min(policy.keepRecentTokens, targetTokens));
		const decisions = [];
		if (!forceSummarize && policy.mode !== "summarize") {
			const through = choosePruneBound(source.content, state, keepRecentTokens);
			if (through > state.clearedThrough) {
				state.clearedThrough = through;
				decisions.push({
					kind: "clear-tool-results",
					through
				});
				if (targetTokens !== void 0 && estimatePromptTokens(buildCompactedView(source.content, state)) <= targetTokens) return Stream.fromIterable(decisions);
			}
		}
		const prune = Stream.fromIterable(decisions);
		if ((!allowSummarize || policy.mode === "prune") && !forceSummarize) return prune;
		return prune.pipe(Stream.concat(Stream.unwrap(Effect.gen(function* () {
			const through = chooseSummarizeCut(source.content, state, keepRecentTokens);
			const covered = collectCoveredMessages(source.content, state, through);
			if (covered.length === 0) return Stream.empty;
			const transcript = renderForSummary(covered, state.summary);
			const prompt = Prompt.fromMessages([Prompt.userMessage({ content: [Prompt.textPart({ text: `${COMPACTION_INSTRUCTION}\n\n<transcript>\n${transcript}\n</transcript>` })] })]);
			const text = yield* request.summarize(prompt, model);
			return Stream.succeed({
				kind: "summarize",
				through,
				summary: text.trim()
			});
		}))));
	})
});
/**
* The sole interpreter compaction port. Runs without an installed service provide layer at their
* composition boundary. Decorators yield this service during construction and receive their
* underlying implementation through Layer.provide. Direct harnesses use the same Layer contract.
*/
var ContextCompactor = class ContextCompactor extends Context.Service()("@effect-agent/engine/ContextCompactor") {
	static layer = Layer.succeed(ContextCompactor, defaultCompactor());
	/** Use the bounded default algorithm with a separate upstream Effect AI Model. */
	static layerWithModel = (model) => Layer.effect(ContextCompactor, Effect.map(model.captureRequirements, (captured) => defaultCompactor(captured)));
};
//#endregion
export { CompactionDecision, CompactionError, ContextCompactor, ContextCompactor_exports as t };

//# sourceMappingURL=ContextCompactor.mjs.map