import { t as __exportAll } from "./rolldown-runtime-D7D4PA-g.mjs";
import "@effect-agent/core/Identifiers";
import "@effect-agent/core/SubagentContract";
import { Context, Schema } from "effect";
//#region src/RunEventSink.ts
var RunEventSink_exports = /* @__PURE__ */ __exportAll({
	RunEventSink: () => RunEventSink,
	RunEventSinkClosedError: () => RunEventSinkClosedError
});
/** A handler emitted a Subagent event after its Tool batch had already settled. */
var RunEventSinkClosedError = class extends Schema.TaggedError()("RunEventSinkClosedError", { message: Schema.String }) {};
/**
* Engine-owned seam through which Tool handlers emit Subagent lifecycle
* events onto the parent Run stream.
*
* The engine provides this service locally to every Tool batch, bound to that
* batch's Run identity and Turn; it is never satisfied from an application
* Layer and is excluded from the runtime's public requirements.
*/
var RunEventSink = class extends Context.Service()("@effect-agent/engine/RunEventSink") {};
//#endregion
export { RunEventSink, RunEventSinkClosedError, RunEventSink_exports as t };

//# sourceMappingURL=RunEventSink.mjs.map