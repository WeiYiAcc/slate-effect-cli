import { RunId, ThreadId } from "@effect-agent/core/Identifiers";
import { Context, Effect, Layer, Schema } from "effect";
import { Prompt } from "effect/unstable/ai";
declare namespace EphemeralThreads_d_exports {
  export { EphemeralThreads, EphemeralThreadsLive, ThreadAppend, ThreadEncodingError, ThreadError, ThreadExport, ThreadHistoryDiverged, ThreadLimitExceeded, ThreadMessage, ThreadNotFound, ThreadSnapshot, ThreadText, threadPrompt };
}
/** Bound applied to one text projection before it can enter a model context. */
declare const ThreadText: Schema.String;
declare const ThreadMessage_base: Schema.Class<ThreadMessage, Schema.Struct<{
  readonly threadId: Schema.brand<Schema.NonEmptyString, "@effect-agent/core/ThreadId">;
  readonly sequence: Schema.Natural;
  readonly runId: Schema.optionalKey<Schema.brand<Schema.NonEmptyString, "@effect-agent/core/RunId">>;
  readonly message: Schema.Codec<Prompt.Message, Prompt.MessageEncoded, never, never>;
  readonly encodedBytes: Schema.Natural;
  readonly timestamp: Schema.DateTimeUtcFromString;
}>, {}>;
/** One append-only native Effect AI message in an ephemeral thread. */
declare class ThreadMessage extends ThreadMessage_base {}
declare const ThreadSnapshot_base: Schema.Class<ThreadSnapshot, Schema.Struct<{
  readonly version: Schema.Literal<1>;
  readonly threadId: Schema.brand<Schema.NonEmptyString, "@effect-agent/core/ThreadId">;
  readonly nextSequence: Schema.Natural;
  readonly contentBytes: Schema.Natural;
  readonly messages: Schema.$Array<typeof ThreadMessage>;
}>, {}>;
/** Immutable view suitable as exact official Prompt history for a subsequent Run. */
declare class ThreadSnapshot extends ThreadSnapshot_base {}
declare const ThreadExport_base: Schema.Class<ThreadExport, Schema.Struct<{
  readonly format: Schema.Literal<"effect-agent/ephemeral-thread@1">;
  readonly exportedAt: Schema.DateTimeUtcFromString;
  readonly snapshot: typeof ThreadSnapshot;
}>, {}>;
/** Portable snapshot of ephemeral state. It is not a durable canonical log. */
declare class ThreadExport extends ThreadExport_base {}
declare const ThreadAppend_base: Schema.Class<ThreadAppend, Schema.Struct<{
  readonly runId: Schema.optionalKey<Schema.brand<Schema.NonEmptyString, "@effect-agent/core/RunId">>;
  readonly message: Schema.Codec<Prompt.Message, Prompt.MessageEncoded, never, never>;
}>, {}>;
/** Native message accepted by the append-only thread. */
declare class ThreadAppend extends ThreadAppend_base {}
declare const ThreadNotFound_base: Schema.Class<ThreadNotFound, Schema.TaggedStruct<"ThreadNotFound", {
  readonly threadId: Schema.brand<Schema.NonEmptyString, "@effect-agent/core/ThreadId">;
}>, import("effect/Cause").YieldableError>;
/** The requested ephemeral Thread has not been created in this process Scope. */
declare class ThreadNotFound extends ThreadNotFound_base {}
declare const ThreadLimitExceeded_base: Schema.Class<ThreadLimitExceeded, Schema.TaggedStruct<"ThreadLimitExceeded", {
  readonly threadId: Schema.brand<Schema.NonEmptyString, "@effect-agent/core/ThreadId">;
  readonly limit: Schema.Literals<readonly ["messages", "content-bytes", "threads", "store-content-bytes"]>;
  readonly limitValue: Schema.Natural;
  readonly observedValue: Schema.Natural;
}>, import("effect/Cause").YieldableError>;
/** A process-local Thread store reached an explicit count or encoded-content bound. */
declare class ThreadLimitExceeded extends ThreadLimitExceeded_base {}
declare const ThreadHistoryDiverged_base: Schema.Class<ThreadHistoryDiverged, Schema.TaggedStruct<"ThreadHistoryDiverged", {
  readonly threadId: Schema.brand<Schema.NonEmptyString, "@effect-agent/core/ThreadId">;
  readonly message: Schema.String;
}>, import("effect/Cause").YieldableError>;
/** Engine history was not an append-only extension of the official snapshot. */
declare class ThreadHistoryDiverged extends ThreadHistoryDiverged_base {}
declare const ThreadEncodingError_base: Schema.Class<ThreadEncodingError, Schema.TaggedStruct<"ThreadEncodingError", {
  readonly threadId: Schema.brand<Schema.NonEmptyString, "@effect-agent/core/ThreadId">;
  readonly message: Schema.String;
}>, import("effect/Cause").YieldableError>;
/** Native Prompt encoding failed before a bounded state mutation. */
declare class ThreadEncodingError extends ThreadEncodingError_base {}
type ThreadError = ThreadNotFound | ThreadLimitExceeded | ThreadHistoryDiverged | ThreadEncodingError;
/** Reconstruct exact Effect AI Prompt history, including tools, reasoning, files, and options. */
declare const threadPrompt: (snapshot: ThreadSnapshot) => Prompt.Prompt;
declare const EphemeralThreads_base: Context.ServiceClass<EphemeralThreads, "@effect-agent/capabilities/EphemeralThreads", {
  readonly create: (threadId: ThreadId) => Effect.Effect<ThreadSnapshot, ThreadLimitExceeded>;
  readonly append: (threadId: ThreadId, message: ThreadAppend) => Effect.Effect<ThreadSnapshot, ThreadNotFound | ThreadLimitExceeded | ThreadEncodingError>;
  /**
   * Record an engine-emitted full history only when it is an append-only
   * extension of the exact official Prompt already stored. The entire
   * suffix commits in one transaction or not at all: concurrent writers are
   * serialized, a writer whose history no longer extends the committed
   * official history fails with ThreadHistoryDiverged, and a limit
   * failure inside the suffix records nothing. Earlier updates remain committed if the Run
   * later fails or is interrupted; this transaction covers one update, not the whole Run.
   */
  readonly recordHistory: (threadId: ThreadId, runId: RunId, history: Prompt.Prompt) => Effect.Effect<ThreadSnapshot, ThreadError>;
  readonly snapshot: (threadId: ThreadId) => Effect.Effect<ThreadSnapshot, ThreadNotFound>;
  readonly export: (threadId: ThreadId) => Effect.Effect<ThreadExport, ThreadNotFound>;
}>;
/**
 * Advanced process-local state for incremental interactive history. Updates remain visible even
 * when a Run later fails; this service has no successful-Run commit boundary or durable recovery.
 * Use ThreadHistory with a memory store for ordinary successful-run retention.
 * Scope closure releases all in-memory state.
 */
declare class EphemeralThreads extends EphemeralThreads_base {}
/** Layer whose state is scoped to the consumer; it intentionally has no persistence semantics. */
declare const EphemeralThreadsLive: Layer.Layer<EphemeralThreads, never, never>;
//#endregion
export { EphemeralThreads, EphemeralThreadsLive, ThreadAppend, ThreadEncodingError, ThreadError, ThreadExport, ThreadHistoryDiverged, ThreadLimitExceeded, ThreadMessage, ThreadNotFound, ThreadSnapshot, ThreadText, EphemeralThreads_d_exports as t, threadPrompt };
//# sourceMappingURL=EphemeralThreads.d.mts.map