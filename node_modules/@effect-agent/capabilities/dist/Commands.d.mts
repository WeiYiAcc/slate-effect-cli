import { Effect, Schema } from "effect";
import { CommandDrainPolicy } from "@effect-agent/engine/RunOptions";
declare namespace Commands_d_exports {
  export { FollowUpCommand, RunCommand, RunCommandQueue, RunCommandQueueClosed, RunCommandQueueConfig, SteeringCommand, makeRunCommandQueue };
}
declare const SteeringCommand_base: Schema.Class<SteeringCommand, Schema.TaggedStruct<"SteeringCommand", {
  readonly id: Schema.NonEmptyString;
  readonly runId: Schema.brand<Schema.NonEmptyString, "@effect-agent/core/RunId">;
  readonly threadId: Schema.brand<Schema.NonEmptyString, "@effect-agent/core/ThreadId">;
  readonly author: Schema.NonEmptyString;
  readonly content: Schema.String;
  readonly createdAt: Schema.DateTimeUtcFromString;
}>, {}>;
/** Commands are delivered only at the engine's documented safe Turn seams. */
declare class SteeringCommand extends SteeringCommand_base {}
declare const FollowUpCommand_base: Schema.Class<FollowUpCommand, Schema.TaggedStruct<"FollowUpCommand", {
  readonly id: Schema.NonEmptyString;
  readonly runId: Schema.brand<Schema.NonEmptyString, "@effect-agent/core/RunId">;
  readonly threadId: Schema.brand<Schema.NonEmptyString, "@effect-agent/core/ThreadId">;
  readonly author: Schema.NonEmptyString;
  readonly content: Schema.String;
  readonly createdAt: Schema.DateTimeUtcFromString;
}>, {}>;
/** A follow-up remains queued until the Run would otherwise stop. */
declare class FollowUpCommand extends FollowUpCommand_base {}
declare const RunCommand: Schema.Union<readonly [typeof SteeringCommand, typeof FollowUpCommand]>;
type RunCommand = typeof RunCommand.Type;
declare const RunCommandQueueConfig_base: Schema.Class<RunCommandQueueConfig, Schema.Struct<{
  readonly capacity: Schema.Int;
}>, {}>;
/** Queue capacity is finite and uses Queue's suspending backpressure strategy. */
declare class RunCommandQueueConfig extends RunCommandQueueConfig_base {}
declare const RunCommandQueueClosed_base: Schema.Class<RunCommandQueueClosed, Schema.TaggedStruct<"RunCommandQueueClosed", {
  readonly runId: Schema.brand<Schema.NonEmptyString, "@effect-agent/core/RunId">;
}>, import("effect/Cause").YieldableError>;
/** A command cannot be admitted after the Run Scope has closed. */
declare class RunCommandQueueClosed extends RunCommandQueueClosed_base {}
/** Scoped command queue for one active Run. */
interface RunCommandQueue {
  readonly offer: (command: RunCommand) => Effect.Effect<void, RunCommandQueueClosed>;
  readonly drain: (policy?: CommandDrainPolicy) => Effect.Effect<ReadonlyArray<RunCommand>>;
  readonly shutdown: Effect.Effect<void>;
}
/**
 * Construct a bounded queue in the current Scope. `shutdown` is registered as
 * a finalizer so awaiting producers are interrupted with their parent Run.
 */
declare const makeRunCommandQueue: (runId: string & import("effect/Brand").Brand<"@effect-agent/core/RunId">, config: RunCommandQueueConfig) => Effect.Effect<{
  offer: (command: RunCommand) => Effect.Effect<void, RunCommandQueueClosed>;
  drain: (policy?: "all" | "one" | undefined) => Effect.Effect<(FollowUpCommand | SteeringCommand)[], never, never>;
  shutdown: Effect.Effect<void, never, never>;
}, never, import("effect/Scope").Scope>;
//#endregion
export { FollowUpCommand, RunCommand, RunCommandQueue, RunCommandQueueClosed, RunCommandQueueConfig, SteeringCommand, makeRunCommandQueue, Commands_d_exports as t };
//# sourceMappingURL=Commands.d.mts.map