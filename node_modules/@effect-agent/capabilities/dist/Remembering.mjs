import { t as __exportAll } from "./rolldown-runtime-D7D4PA-g.mjs";
import { Effect, Encoding, Schema } from "effect";
import { MemoryContent, MemorySourceReference } from "@effect-agent/core/MemoryReference";
import { MemoryDocument, MemoryKey, MemoryReader, MemoryScope, MemoryWrite, MemoryWriter } from "@effect-agent/core/MemoryStore";
import * as Protocol from "@effect-agent/core/RememberingStore";
//#region src/Remembering.ts
var Remembering_exports = /* @__PURE__ */ __exportAll({
	Decision: () => Decision,
	Limits: () => Limits,
	SourceSnapshot: () => SourceSnapshot,
	admit: () => admit,
	invalidate: () => invalidate,
	make: () => make
});
var Limits = class extends Schema.Class("@effect-agent/remembering/Limits")({
	maxSourceBytes: Schema.Int.check(Schema.isBetween({
		minimum: 1,
		maximum: 4194304
	})),
	maxProposalBytes: Schema.Int.check(Schema.isBetween({
		minimum: 1,
		maximum: 1048576
	})),
	timeoutMillis: Schema.Int.check(Schema.isBetween({
		minimum: 1,
		maximum: 3e5
	}))
}) {};
var SourceSnapshot = class extends Schema.Class("@effect-agent/remembering/SourceSnapshot")({
	source: Protocol.Source,
	text: Schema.String.check(Schema.isMaxLength(4194304))
}) {};
const SourceResult = Schema.NullOr(Schema.Union([SourceSnapshot, Protocol.Invalidation]));
/** Application-authorized output. The coordinator binds key, operation ID and expected revision.
* Fact acceptance, sharing scopes, profile schema and source-aware merging remain application code.
*/
const Decision = Schema.Union([
	Schema.TaggedStruct("Put", {
		locator: MemorySourceReference.fields.locator,
		content: MemoryContent,
		scopes: Schema.Array(MemoryScope).check(Schema.isMaxLength(128))
	}),
	Schema.TaggedStruct("Withdraw", { reason: Schema.String.check(Schema.isMaxLength(4096)) }),
	Schema.TaggedStruct("NoChange", {}),
	Schema.TaggedStruct("Reject", {})
]);
const equal = (left, right) => JSON.stringify(left) === JSON.stringify(right);
const sameKey = (left, right) => left.id === right.id && left.namespace.address === right.namespace.address;
const encodeIntent = Schema.encodeSync(Protocol.Intent.Wire);
const byteLength = (text) => Encoding.encodeHex(text).length / 2;
const validateSource = Effect.fn("Remembering.validateSource")(function* (intent, snapshot, limits) {
	const source = yield* Schema.decodeUnknownEffect(SourceSnapshot)(snapshot).pipe(Effect.mapError(() => Protocol.ProcessingError.make({ reason: "invalid-input" })));
	const actual = yield* Schema.encodeEffect(Protocol.Source)(source.source);
	const expected = yield* Schema.encodeEffect(Protocol.Source)(intent.source);
	if (!equal(actual, expected)) return yield* Protocol.ProcessingError.make({ reason: "source-changed" });
	if (byteLength(source.text) > limits.maxSourceBytes) return yield* Protocol.ProcessingError.make({ reason: "budget" });
	return source;
});
const validateEvidence = Effect.fn("Remembering.validateEvidence")(function* (source, proposal) {
	const bytes = Encoding.encodeHex(source.text);
	for (const evidence of proposal.evidence) {
		const quote = Encoding.encodeHex(evidence.quote);
		if (evidence.source.id !== source.source.key.id || evidence.source.revision !== source.source.revision || evidence.source.locator !== source.source.locator || evidence.endByte <= evidence.startByte || evidence.endByte > bytes.length / 2 || quote.length / 2 !== evidence.endByte - evidence.startByte || quote !== bytes.slice(evidence.startByte * 2, evidence.endByte * 2)) return yield* Protocol.ProcessingError.make({ reason: "invalid-evidence" });
	}
});
const validateCheckpoint = Effect.fn("Remembering.validateCheckpoint")(function* (intent, input) {
	const checkpoint = yield* Schema.decodeUnknownEffect(Protocol.Checkpoint)(input).pipe(Effect.mapError(() => Protocol.CheckpointError.make({ reason: "corrupt" })));
	if (!equal(encodeIntent(intent), encodeIntent(checkpoint.intent)) || checkpoint.suppression !== null && !sameKey(checkpoint.suppression.source, intent.source.key)) return yield* Protocol.CheckpointError.make({ reason: "corrupt" });
	const progress = checkpoint.progress;
	if (progress._tag !== "Pending" && progress.applied !== null && !sameKey(progress.applied.key, intent.target)) return yield* Protocol.CheckpointError.make({ reason: "corrupt" });
	if (progress._tag === "Prepared" && (!sameKey(progress.command.key, intent.target) || progress.command.operationId !== operationId(intent, progress.purpose, progress.attempt))) return yield* Protocol.CheckpointError.make({ reason: "corrupt" });
	const applied = progress._tag === "Pending" || progress.applied === null ? null : yield* MemoryDocument.restore(intent.target.namespace, progress.applied).pipe(Effect.mapError(() => Protocol.CheckpointError.make({ reason: "corrupt" })));
	const boundProgress = progress._tag === "Pending" ? progress : progress._tag === "Prepared" ? {
		...progress,
		applied,
		command: MemoryWrite.make({
			...progress.command,
			key: intent.target
		})
	} : {
		...progress,
		applied
	};
	const suppression = checkpoint.suppression === null ? null : Object.assign(checkpoint.suppression, { source: MemoryKey.make({
		...checkpoint.suppression.source,
		namespace: intent.source.key.namespace
	}) });
	return Object.assign(checkpoint, {
		intent,
		progress: boundProgress,
		suppression
	});
});
const operationId = (intent, purpose, attempt) => `remembering/${intent.id}/${purpose}/${attempt}`;
const completed = (progress, outcome, cleaned, applied) => ({
	_tag: "Completed",
	proposal: progress._tag === "Pending" ? null : progress.proposal,
	attempt: progress._tag === "Pending" ? 0 : progress.attempt,
	applied: applied ?? (progress._tag === "Pending" ? null : progress.applied),
	outcome,
	cleaned
});
/** Foreground admission performs only one durable port call. Bind authority and idempotency
* from the trusted invocation before calling; a queued receipt is returned only after commit.
*/
const admit = Effect.fn("Remembering.admit")(function* (store, intent) {
	const checked = yield* Schema.decodeUnknownEffect(Protocol.Intent.Wire)(intent).pipe(Effect.mapError(() => Protocol.AdmissionError.make({ reason: "invalid-input" })));
	const failpoint = yield* Protocol.MutationFailpoint;
	yield* failpoint.hit("admission:before");
	const receipt = yield* store.admit(checked);
	yield* failpoint.hit("admission:after");
	return receipt;
});
/** Source owners await this durable suppression receipt before acknowledging edit/deletion/Forget.
* Scheduling all affected retained references and conditional cleanup is the host's obligation.
*/
const invalidate = Effect.fn("Remembering.invalidate")(function* (store, event) {
	const checked = yield* Schema.decodeUnknownEffect(Protocol.Invalidation)(event).pipe(Effect.mapError(() => Protocol.AdmissionError.make({ reason: "invalid-input" })));
	const failpoint = yield* Protocol.MutationFailpoint;
	yield* failpoint.hit("suppression:before");
	const receipt = yield* store.invalidate(checked);
	yield* failpoint.hit("suppression:after");
	return receipt;
});
/** Define one application learner. Extraction may call native Effect AI LanguageModel directly;
* callback/model/schema requirements and errors remain in the returned Effect. No model owns
* authority, source loading, fact acceptance, profile representation or sharing policy.
* These are Effect-composition callbacks: they can yield application Context services, and advance
* retains every callback's E/R. The framework neither acquires nor provides application policy.
*
* advance is finite: it extracts, prepares, dispatches, or rebases at most once. The host calls it
* again when ready. A proposal is persisted before any target read; a complete command is saved
* before dispatch. Unknown outcomes leave that command untouched. Only MemoryConflict rebases.
* Cleanup receives the immutable proposal, original applied document and latest target so it can
* remove that contribution without overwriting intervening human corrections.
*
* loadSource returns an explicit Invalidation when source authority changed or was revoked; its
* suppression is committed before processing returns. null means absent on an initial load. A
* later null or a changed snapshot without an authority event fails source-changed and retains
* the obligation. Sources must keep revision content immutable. Evidence is checked again before
* preparing a new command. A saved Prepared command may already have executed, so it never calls
* loadSource and must reconcile even during a source outage. Source owners durably invalidate
* before acknowledging edits, and authoritative recall filters suppression while cleanup runs.
* No source text, proposal, authority identity, or extracted values are added to tracing spans.
*/
const make = (options) => {
	return { advance: Effect.fn("Remembering.advance")(function* (input) {
		const limits = yield* Schema.decodeUnknownEffect(Limits)(input.limits).pipe(Effect.mapError(() => Protocol.ProcessingError.make({ reason: "invalid-input" })));
		const decodedIntent = yield* Schema.decodeUnknownEffect(Protocol.Intent.Wire)(input.intent).pipe(Effect.mapError(() => Protocol.ProcessingError.make({ reason: "invalid-input" })));
		const intent = Object.assign(decodedIntent, {
			source: {
				...decodedIntent.source,
				key: MemoryKey.make({
					...decodedIntent.source.key,
					namespace: input.intent.source.key.namespace
				})
			},
			target: MemoryKey.make({
				...decodedIntent.target,
				namespace: input.intent.target.namespace
			})
		});
		return yield* Effect.gen(function* () {
			const checkpoint = yield* input.store.read(intent).pipe(Effect.flatMap((value) => validateCheckpoint(intent, value)));
			const progress = checkpoint.progress;
			const suppression = checkpoint.suppression;
			if (progress._tag === "Proposed" && suppression === null && byteLength(JSON.stringify(progress.proposal)) > limits.maxProposalBytes) return yield* Protocol.ProcessingError.make({ reason: "budget" });
			const failpoint = yield* Protocol.MutationFailpoint;
			const save = Effect.fn("Remembering.save")(function* (next, before, after) {
				yield* failpoint.hit(before);
				const stored = yield* input.store.save({
					intent,
					expectedVersion: checkpoint.version,
					progress: next
				});
				yield* failpoint.hit(after);
				return yield* validateCheckpoint(intent, stored);
			});
			const suppress = Effect.fn("Remembering.suppress")(function* (event) {
				if (!sameKey(event.source, intent.source.key)) return yield* Protocol.ProcessingError.make({ reason: "invalid-input" });
				yield* invalidate(input.store, event);
				return yield* input.store.read(intent).pipe(Effect.flatMap((value) => validateCheckpoint(intent, value)));
			});
			if (progress._tag === "Completed" && (suppression === null || progress.cleaned)) return checkpoint;
			if (suppression !== null && progress._tag !== "Prepared" && (progress._tag === "Pending" || progress.applied === null)) return yield* save(completed(progress, "suppressed", true), "completion:before", "completion:after");
			if (progress._tag === "Pending") {
				if (!input.extractionEnabled) return yield* Protocol.ProcessingError.make({ reason: "disabled" });
				const snapshot = yield* options.loadSource(intent).pipe(Effect.flatMap(Schema.decodeUnknownEffect(SourceResult)));
				if (snapshot === null) return yield* save(completed(progress, "rejected", false), "completion:before", "completion:after");
				if (Schema.is(Protocol.Invalidation)(snapshot)) return yield* suppress(snapshot);
				const source = yield* validateSource(intent, snapshot, limits);
				const extracted = yield* options.extract(source, intent);
				if (extracted === null) return yield* save(completed(progress, "no-change", false), "completion:before", "completion:after");
				const value = yield* Schema.encodeEffect(options.proposal)(extracted.value);
				const proposal = yield* Schema.decodeUnknownEffect(Protocol.Proposal)({
					version: 1,
					value,
					evidence: extracted.evidence
				});
				if (byteLength(JSON.stringify(proposal)) > limits.maxProposalBytes) return yield* Protocol.ProcessingError.make({ reason: "budget" });
				yield* validateEvidence(source, proposal);
				const latest = yield* options.loadSource(intent).pipe(Effect.flatMap(Schema.decodeUnknownEffect(SourceResult)));
				if (latest === null) return yield* Protocol.ProcessingError.make({ reason: "source-changed" });
				if (Schema.is(Protocol.Invalidation)(latest)) return yield* suppress(latest);
				yield* validateSource(intent, latest, limits);
				if (latest.text !== source.text) return yield* Protocol.ProcessingError.make({ reason: "source-changed" });
				return yield* save({
					_tag: "Proposed",
					proposal,
					attempt: 0,
					purpose: "remember",
					applied: null
				}, "proposal:before", "proposal:after");
			}
			if (progress._tag === "Prepared") {
				const writer = yield* MemoryWriter;
				const command = MemoryWrite.make({
					...progress.command,
					key: intent.target
				});
				const outcome = yield* writer.change(command).pipe(Effect.map((document) => ({
					_tag: "Written",
					document
				})), Effect.catchTag("MemoryConflict", () => Effect.succeed({ _tag: "Conflict" })), Effect.catchTag("MemoryWithdrawn", () => Effect.succeed({ _tag: "Withdrawn" })));
				if (outcome._tag === "Written") return yield* save(completed(progress, progress.purpose === "cleanup" ? "suppressed" : "applied", progress.purpose === "cleanup", progress.purpose === "remember" ? outcome.document : void 0), "completion:before", "completion:after");
				if (outcome._tag === "Withdrawn" || suppression !== null && progress.purpose === "remember") return yield* save(completed(progress, suppression !== null ? "suppressed" : "rejected", suppression !== null), "completion:before", "completion:after");
				return yield* save({
					_tag: "Proposed",
					proposal: progress.proposal,
					attempt: progress.attempt,
					purpose: progress.purpose,
					applied: progress.applied
				}, "rebase:before", "rebase:after");
			}
			const proposal = progress.proposal;
			if (proposal === null) return yield* Protocol.CheckpointError.make({ reason: "corrupt" });
			if (suppression === null) {
				const latest = yield* options.loadSource(intent).pipe(Effect.flatMap(Schema.decodeUnknownEffect(SourceResult)));
				if (latest === null) return yield* Protocol.ProcessingError.make({ reason: "source-changed" });
				if (Schema.is(Protocol.Invalidation)(latest)) return yield* suppress(latest);
				yield* validateSource(intent, latest, limits);
				yield* validateEvidence(latest, proposal);
			}
			const value = yield* Schema.decodeUnknownEffect(options.proposal)(proposal.value);
			const current = yield* (yield* MemoryReader).get(intent.target);
			const applied = progress.applied === null ? null : yield* MemoryDocument.restore(intent.target.namespace, progress.applied);
			const cleanup = suppression !== null && applied !== null;
			const mergeInput = {
				intent,
				proposal: {
					value,
					evidence: proposal.evidence
				},
				current
			};
			const candidate = cleanup ? yield* options.cleanup({
				...mergeInput,
				suppression,
				applied
			}) : yield* options.merge(mergeInput);
			const decision = yield* Schema.decodeUnknownEffect(Decision)(candidate);
			if (cleanup && decision._tag === "Reject") return yield* Protocol.ProcessingError.make({ reason: "cleanup-rejected" });
			if (decision._tag === "NoChange" || decision._tag === "Reject") return yield* save(completed(progress, cleanup ? "suppressed" : decision._tag === "Reject" ? "rejected" : "no-change", cleanup, !cleanup && decision._tag === "NoChange" && current !== null ? current : void 0), cleanup ? "cleanup:before" : "completion:before", cleanup ? "cleanup:after" : "completion:after");
			if (decision._tag === "Withdraw" && current === null) return yield* save(completed(progress, cleanup ? "suppressed" : "no-change", cleanup), "completion:before", "completion:after");
			const purpose = cleanup ? "cleanup" : "remember";
			const attempt = progress.attempt + 1;
			const key = intent.target;
			return yield* save({
				_tag: "Prepared",
				proposal,
				applied,
				attempt,
				purpose,
				command: yield* Schema.decodeUnknownEffect(MemoryWrite.Wire)({
					...decision,
					key,
					operationId: operationId(intent, purpose, attempt),
					expectedRevision: current?.source.revision ?? null
				})
			}, cleanup ? "cleanup:before" : "command:before", cleanup ? "cleanup:after" : "command:after");
		}).pipe(Effect.timeoutOrElse({
			duration: limits.timeoutMillis,
			orElse: () => Protocol.ProcessingError.make({ reason: "timeout" })
		}));
	}) };
};
//#endregion
export { Decision, Limits, SourceSnapshot, admit, invalidate, make, Remembering_exports as t };

//# sourceMappingURL=Remembering.mjs.map