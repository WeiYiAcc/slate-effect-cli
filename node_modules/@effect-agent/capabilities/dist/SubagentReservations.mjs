import { t as __exportAll } from "./rolldown-runtime-D7D4PA-g.mjs";
import { RunId, ToolCallId } from "@effect-agent/core/Identifiers";
import { Context, Effect, Layer, Ref, Schema, Semaphore } from "effect";
import { SubagentDelegationCaps, SubagentReservationAmounts } from "@effect-agent/core/SubagentContract";
//#region src/SubagentReservations.ts
var SubagentReservations_exports = /* @__PURE__ */ __exportAll({
	BudgetReservationId: () => BudgetReservationId,
	SubagentAvailableAmounts: () => SubagentAvailableAmounts,
	SubagentBudgetDimension: () => SubagentBudgetDimension,
	SubagentBudgetExhausted: () => SubagentBudgetExhausted,
	SubagentObservedUsage: () => SubagentObservedUsage,
	SubagentParentBudgetActive: () => SubagentParentBudgetActive,
	SubagentParentBudgetConflict: () => SubagentParentBudgetConflict,
	SubagentParentBudgetUnknown: () => SubagentParentBudgetUnknown,
	SubagentParentBudgetView: () => SubagentParentBudgetView,
	SubagentReservationConflict: () => SubagentReservationConflict,
	SubagentReservationRequest: () => SubagentReservationRequest,
	SubagentReservationStatus: () => SubagentReservationStatus,
	SubagentReservationUnknown: () => SubagentReservationUnknown,
	SubagentReservationView: () => SubagentReservationView,
	SubagentReservations: () => SubagentReservations,
	SubagentReservationsMemoryLive: () => SubagentReservationsMemoryLive,
	makeBudgetReservationId: () => makeBudgetReservationId
});
const Natural = Schema.Natural;
/**
* Stable identity of one parent-owned Subagent budget reservation.
*
* Derivation rule (see {@link makeBudgetReservationId}):
* `encodeURIComponent(parentRunId) + ":" + encodeURIComponent(parentToolCallId)`.
* Component encoding escapes `:`; the mapping is injective for every
* `(parentRunId, parentToolCallId)` pair.
*/
const BudgetReservationId = Schema.NonEmptyString.pipe(Schema.brand("@effect-agent/capabilities/BudgetReservationId"));
const decodeBudgetReservationId = Schema.decodeSync(BudgetReservationId);
/** Derive the stable reservation identity of one Subagent Invocation. */
const makeBudgetReservationId = (parentRunId, parentToolCallId) => decodeBudgetReservationId(`${encodeURIComponent(parentRunId)}:${encodeURIComponent(parentToolCallId)}`);
/** One enforced hierarchical delegation budget dimension. */
const SubagentBudgetDimension = Schema.Literals([
	"total-child-invocations",
	"concurrent-children",
	"turns",
	"tool-calls",
	"duration",
	"input-tokens",
	"output-tokens",
	"cost",
	"result-bytes"
]);
SubagentReservationAmounts.fields;
const OptionalAmountFields = {
	turns: Schema.optionalKey(Natural),
	toolCalls: Schema.optionalKey(Natural),
	durationMillis: Schema.optionalKey(Natural),
	inputTokens: Schema.optionalKey(Natural),
	outputTokens: Schema.optionalKey(Natural),
	costMicrousd: Schema.optionalKey(Natural),
	resultBytes: Schema.optionalKey(Natural)
};
/** Observed child usage. An absent dimension was never reported and settles conservatively. */
var SubagentObservedUsage = class extends Schema.Class("@effect-agent/capabilities/SubagentObservedUsage")(OptionalAmountFields) {};
/** Remaining delegable allocation per configured dimension. Absent means not configured. */
var SubagentAvailableAmounts = class extends Schema.Class("@effect-agent/capabilities/SubagentAvailableAmounts")(OptionalAmountFields) {};
/** Reservation lifecycle: absent → reserved → releasePending → released. */
const SubagentReservationStatus = Schema.Literals([
	"reserved",
	"releasePending",
	"released"
]);
/** All-or-nothing allocation request for one Subagent Invocation. */
var SubagentReservationRequest = class extends Schema.Class("@effect-agent/capabilities/SubagentReservationRequest")({
	parentRunId: RunId,
	parentToolCallId: ToolCallId,
	allocation: SubagentReservationAmounts
}) {};
/** Accounting snapshot of one reservation: allocated, observed, covered, released, and overrun. */
var SubagentReservationView = class extends Schema.Class("@effect-agent/capabilities/SubagentReservationView")({
	reservationId: BudgetReservationId,
	parentRunId: RunId,
	parentToolCallId: ToolCallId,
	status: SubagentReservationStatus,
	allocated: SubagentReservationAmounts,
	observedConsumed: SubagentObservedUsage,
	coveredConsumed: SubagentReservationAmounts,
	released: SubagentReservationAmounts,
	overrun: SubagentReservationAmounts
}) {};
/** Aggregate parent delegation budget snapshot including every reservation it owns. */
var SubagentParentBudgetView = class extends Schema.Class("@effect-agent/capabilities/SubagentParentBudgetView")({
	parentRunId: RunId,
	caps: SubagentDelegationCaps,
	totalChildInvocations: Natural,
	available: SubagentAvailableAmounts,
	cumulativeOverrun: SubagentReservationAmounts,
	reservations: Schema.Array(SubagentReservationView)
}) {};
/** All-or-nothing admission failed: one dimension cannot cover the requested allocation. */
var SubagentBudgetExhausted = class extends Schema.TaggedError()("SubagentBudgetExhausted", {
	parentRunId: RunId,
	dimension: SubagentBudgetDimension,
	limitValue: Natural,
	observedValue: Natural
}) {};
/** The stable key is already reserved with a different allocation; neither request may win silently. */
var SubagentReservationConflict = class extends Schema.TaggedError()("SubagentReservationConflict", {
	reservationId: BudgetReservationId,
	existingAllocation: SubagentReservationAmounts,
	requestedAllocation: SubagentReservationAmounts
}) {};
/** No reservation exists for this identity. */
var SubagentReservationUnknown = class extends Schema.TaggedError()("SubagentReservationUnknown", { reservationId: BudgetReservationId }) {};
/** No delegation budget was registered for this parent Run. */
var SubagentParentBudgetUnknown = class extends Schema.TaggedError()("SubagentParentBudgetUnknown", { parentRunId: RunId }) {};
/** The parent Run is already registered with different caps; neither set may win silently. */
var SubagentParentBudgetConflict = class extends Schema.TaggedError()("SubagentParentBudgetConflict", { parentRunId: RunId }) {};
/** A parent budget cannot retire while one of its child reservations is unsettled. */
var SubagentParentBudgetActive = class extends Schema.TaggedError()("SubagentParentBudgetActive", {
	parentRunId: RunId,
	openReservations: Natural
}) {};
/**
* Parent-owned hierarchical Subagent budget reservation authority.
*
* Every transition is a single atomic `Ref` update, so parallel children can
* never observe and spend the same remainder. The post-response usage hook in
* `budget.ts` is deliberately not reused here: it accounts after the fact and
* is not a reservation service.
*/
var SubagentReservations = class extends Context.Service()("@effect-agent/capabilities/SubagentReservations") {};
const dimensionSpecs = [
	{
		key: "turns",
		dimension: "turns",
		cap: (caps) => caps.maxTurns
	},
	{
		key: "toolCalls",
		dimension: "tool-calls",
		cap: (caps) => caps.maxToolCalls
	},
	{
		key: "durationMillis",
		dimension: "duration",
		cap: (caps) => caps.maxDurationMillis
	},
	{
		key: "inputTokens",
		dimension: "input-tokens",
		cap: (caps) => caps.maxInputTokens
	},
	{
		key: "outputTokens",
		dimension: "output-tokens",
		cap: (caps) => caps.maxOutputTokens
	},
	{
		key: "costMicrousd",
		dimension: "cost",
		cap: (caps) => caps.maxCostMicrousd
	},
	{
		key: "resultBytes",
		dimension: "result-bytes",
		cap: (caps) => caps.maxResultBytes
	}
];
const zeroAmounts = {
	turns: 0,
	toolCalls: 0,
	durationMillis: 0,
	inputTokens: 0,
	outputTokens: 0,
	costMicrousd: 0,
	resultBytes: 0
};
const ok = (value) => ({
	_tag: "ok",
	value
});
const fail = (error) => ({
	_tag: "error",
	error
});
const resolve = (result) => {
	switch (result._tag) {
		case "ok": return Effect.succeed(result.value);
		case "error": return Effect.fail(result.error);
		case "corrupt": return Effect.die(new Error(result.message));
	}
};
const sameCaps = (a, b) => a.maxTotalChildInvocations === b.maxTotalChildInvocations && a.maxConcurrentChildren === b.maxConcurrentChildren && a.maxTurns === b.maxTurns && a.maxToolCalls === b.maxToolCalls && a.maxDurationMillis === b.maxDurationMillis && a.maxInputTokens === b.maxInputTokens && a.maxOutputTokens === b.maxOutputTokens && a.maxCostMicrousd === b.maxCostMicrousd && a.maxResultBytes === b.maxResultBytes;
const sameAmounts = (a, b) => dimensionSpecs.every((spec) => a[spec.key] === b[spec.key]);
const optionalAmounts = (partial) => {
	const out = {};
	for (const spec of dimensionSpecs) {
		const value = partial[spec.key];
		if (value !== void 0) out[spec.key] = value;
	}
	return out;
};
const reservationView = (reservationId, state) => SubagentReservationView.make({
	reservationId,
	parentRunId: state.parentRunId,
	parentToolCallId: state.parentToolCallId,
	status: state.status,
	allocated: SubagentReservationAmounts.make(state.allocated),
	observedConsumed: SubagentObservedUsage.make(optionalAmounts(state.observed)),
	coveredConsumed: SubagentReservationAmounts.make(state.covered),
	released: SubagentReservationAmounts.make(state.released),
	overrun: SubagentReservationAmounts.make(state.overrun)
});
const parentView = (ledger, parent) => {
	const reservations = [];
	for (const [reservationId, reservation] of ledger.reservations) if (reservation.parentRunId === parent.parentRunId) reservations.push(reservationView(reservationId, reservation));
	return SubagentParentBudgetView.make({
		parentRunId: parent.parentRunId,
		caps: parent.caps,
		totalChildInvocations: parent.totalChildInvocations,
		available: SubagentAvailableAmounts.make(optionalAmounts(parent.available)),
		cumulativeOverrun: SubagentReservationAmounts.make(parent.cumulativeOverrun),
		reservations
	});
};
const corruptParent = (parentRunId) => ({
	_tag: "corrupt",
	message: `SubagentReservations ledger invariant violated: reservation references unregistered parent Run ${parentRunId}`
});
const registerTransition = (ledger, parentRunId, caps, gate) => {
	const existing = ledger.parents.get(parentRunId);
	if (existing !== void 0) return sameCaps(existing.caps, caps) ? [ok(parentView(ledger, existing)), ledger] : [fail(SubagentParentBudgetConflict.make({ parentRunId })), ledger];
	const available = {};
	for (const spec of dimensionSpecs) {
		const cap = spec.cap(caps);
		if (cap !== void 0) available[spec.key] = cap;
	}
	const parent = {
		parentRunId,
		caps,
		gate,
		totalChildInvocations: 0,
		available,
		cumulativeOverrun: zeroAmounts
	};
	const next = {
		parents: new Map(ledger.parents).set(parentRunId, parent),
		reservations: ledger.reservations
	};
	return [ok(parentView(next, parent)), next];
};
const reserveTransition = (ledger, request) => {
	const parent = ledger.parents.get(request.parentRunId);
	if (parent === void 0) return [fail(SubagentParentBudgetUnknown.make({ parentRunId: request.parentRunId })), ledger];
	const reservationId = makeBudgetReservationId(request.parentRunId, request.parentToolCallId);
	const existing = ledger.reservations.get(reservationId);
	if (existing !== void 0) return sameAmounts(existing.allocated, request.allocation) ? [ok(reservationView(reservationId, existing)), ledger] : [fail(SubagentReservationConflict.make({
		reservationId,
		existingAllocation: SubagentReservationAmounts.make(existing.allocated),
		requestedAllocation: request.allocation
	})), ledger];
	const maxInvocations = parent.caps.maxTotalChildInvocations;
	if (maxInvocations !== void 0 && parent.totalChildInvocations + 1 > maxInvocations) return [fail(SubagentBudgetExhausted.make({
		parentRunId: request.parentRunId,
		dimension: "total-child-invocations",
		limitValue: maxInvocations,
		observedValue: parent.totalChildInvocations + 1
	})), ledger];
	for (const spec of dimensionSpecs) {
		const cap = spec.cap(parent.caps);
		if (cap === void 0) continue;
		const headroom = (parent.available[spec.key] ?? 0) - parent.cumulativeOverrun[spec.key];
		const requested = request.allocation[spec.key];
		if (requested > headroom) return [fail(SubagentBudgetExhausted.make({
			parentRunId: request.parentRunId,
			dimension: spec.dimension,
			limitValue: cap,
			observedValue: cap - headroom + requested
		})), ledger];
	}
	const nextAvailable = { ...parent.available };
	for (const spec of dimensionSpecs) {
		const available = parent.available[spec.key];
		if (available !== void 0) nextAvailable[spec.key] = available - request.allocation[spec.key];
	}
	const reservation = {
		parentRunId: request.parentRunId,
		parentToolCallId: request.parentToolCallId,
		status: "reserved",
		allocated: request.allocation,
		observed: {},
		covered: zeroAmounts,
		overrun: zeroAmounts,
		releasable: zeroAmounts,
		released: zeroAmounts
	};
	const next = {
		parents: new Map(ledger.parents).set(request.parentRunId, {
			...parent,
			totalChildInvocations: parent.totalChildInvocations + 1,
			available: nextAvailable
		}),
		reservations: new Map(ledger.reservations).set(reservationId, reservation)
	};
	return [ok(reservationView(reservationId, reservation)), next];
};
const observeTransition = (ledger, reservationId, usage) => {
	const reservation = ledger.reservations.get(reservationId);
	if (reservation === void 0) return [fail(SubagentReservationUnknown.make({ reservationId })), ledger];
	const parent = ledger.parents.get(reservation.parentRunId);
	if (parent === void 0) return [corruptParent(reservation.parentRunId), ledger];
	const observed = { ...reservation.observed };
	const covered = { ...reservation.covered };
	const overrun = { ...reservation.overrun };
	const cumulativeOverrun = { ...parent.cumulativeOverrun };
	for (const spec of dimensionSpecs) {
		const delta = usage[spec.key];
		if (delta === void 0) continue;
		observed[spec.key] = (observed[spec.key] ?? 0) + delta;
		const coverable = reservation.status === "reserved" ? Math.min(delta, reservation.allocated[spec.key] - covered[spec.key]) : 0;
		covered[spec.key] += coverable;
		const overrunDelta = delta - coverable;
		overrun[spec.key] += overrunDelta;
		cumulativeOverrun[spec.key] += overrunDelta;
	}
	const nextReservation = {
		...reservation,
		observed,
		covered,
		overrun
	};
	const next = {
		parents: new Map(ledger.parents).set(parent.parentRunId, {
			...parent,
			cumulativeOverrun
		}),
		reservations: new Map(ledger.reservations).set(reservationId, nextReservation)
	};
	return [ok(reservationView(reservationId, nextReservation)), next];
};
/**
* Freeze the settlement decision. A dimension that was never observed
* conservatively consumes its full reserved amount.
*/
const freezeSettlement = (reservation) => {
	const observed = { ...reservation.observed };
	const covered = { ...reservation.covered };
	const releasable = { ...zeroAmounts };
	for (const spec of dimensionSpecs) {
		if (observed[spec.key] === void 0) {
			observed[spec.key] = reservation.allocated[spec.key];
			covered[spec.key] = reservation.allocated[spec.key];
		}
		releasable[spec.key] = reservation.allocated[spec.key] - covered[spec.key];
	}
	return {
		...reservation,
		status: "releasePending",
		observed,
		covered,
		releasable
	};
};
const beginReleaseTransition = (ledger, reservationId) => {
	const reservation = ledger.reservations.get(reservationId);
	if (reservation === void 0) return [fail(SubagentReservationUnknown.make({ reservationId })), ledger];
	if (reservation.status !== "reserved") return [ok(reservationView(reservationId, reservation)), ledger];
	const frozen = freezeSettlement(reservation);
	const next = {
		parents: ledger.parents,
		reservations: new Map(ledger.reservations).set(reservationId, frozen)
	};
	return [ok(reservationView(reservationId, frozen)), next];
};
const releaseTransition = (ledger, reservationId) => {
	const reservation = ledger.reservations.get(reservationId);
	if (reservation === void 0) return [fail(SubagentReservationUnknown.make({ reservationId })), ledger];
	if (reservation.status === "released") return [ok(reservationView(reservationId, reservation)), ledger];
	const parent = ledger.parents.get(reservation.parentRunId);
	if (parent === void 0) return [corruptParent(reservation.parentRunId), ledger];
	const frozen = reservation.status === "reserved" ? freezeSettlement(reservation) : reservation;
	const nextAvailable = { ...parent.available };
	for (const spec of dimensionSpecs) {
		const available = parent.available[spec.key];
		if (available !== void 0) nextAvailable[spec.key] = available + frozen.releasable[spec.key];
	}
	const settled = {
		...frozen,
		status: "released",
		released: frozen.releasable
	};
	const next = {
		parents: new Map(ledger.parents).set(parent.parentRunId, {
			...parent,
			available: nextAvailable
		}),
		reservations: new Map(ledger.reservations).set(reservationId, settled)
	};
	return [ok(reservationView(reservationId, settled)), next];
};
const retireParentTransition = (ledger, parentRunId) => {
	if (!ledger.parents.has(parentRunId)) return [ok(void 0), ledger];
	let openReservations = 0;
	for (const reservation of ledger.reservations.values()) if (reservation.parentRunId === parentRunId && reservation.status !== "released") openReservations += 1;
	if (openReservations > 0) return [fail(SubagentParentBudgetActive.make({
		parentRunId,
		openReservations
	})), ledger];
	const parents = new Map(ledger.parents);
	parents.delete(parentRunId);
	const reservations = new Map(ledger.reservations);
	for (const [reservationId, reservation] of reservations) if (reservation.parentRunId === parentRunId) reservations.delete(reservationId);
	return [ok(void 0), {
		parents,
		reservations
	}];
};
/**
* In-memory reservation ledger. All state lives in one `Ref` owned by the
* Layer's Scope; every transition is a single atomic `Ref.modify`.
*/
const SubagentReservationsMemoryLive = Layer.effect(SubagentReservations, Effect.gen(function* () {
	const state = yield* Ref.make({
		parents: /* @__PURE__ */ new Map(),
		reservations: /* @__PURE__ */ new Map()
	});
	return SubagentReservations.of({
		registerParent: Effect.fn("SubagentReservations.registerParent")(function* (parentRunId, caps) {
			const gate = caps.maxConcurrentChildren !== void 0 && caps.maxConcurrentChildren > 0 ? yield* Semaphore.make(caps.maxConcurrentChildren) : void 0;
			const result = yield* Ref.modify(state, (ledger) => registerTransition(ledger, parentRunId, caps, gate));
			return yield* resolve(result);
		}),
		reserve: Effect.fn("SubagentReservations.reserve")(function* (request) {
			const result = yield* Ref.modify(state, (ledger) => reserveTransition(ledger, request));
			return yield* resolve(result);
		}),
		observe: Effect.fn("SubagentReservations.observe")(function* (reservationId, usage) {
			const result = yield* Ref.modify(state, (ledger) => observeTransition(ledger, reservationId, usage));
			return yield* resolve(result);
		}),
		beginRelease: Effect.fn("SubagentReservations.beginRelease")(function* (reservationId) {
			const result = yield* Ref.modify(state, (ledger) => beginReleaseTransition(ledger, reservationId));
			return yield* resolve(result);
		}),
		release: Effect.fn("SubagentReservations.release")(function* (reservationId) {
			const result = yield* Ref.modify(state, (ledger) => releaseTransition(ledger, reservationId));
			return yield* resolve(result);
		}),
		acquireChildSlot: Effect.fn("SubagentReservations.acquireChildSlot")(function* (parentRunId) {
			const parent = (yield* Ref.get(state)).parents.get(parentRunId);
			if (parent === void 0) return yield* SubagentParentBudgetUnknown.make({ parentRunId });
			const cap = parent.caps.maxConcurrentChildren;
			if (cap === void 0) return;
			if (cap === 0 || parent.gate === void 0) return yield* SubagentBudgetExhausted.make({
				parentRunId,
				dimension: "concurrent-children",
				limitValue: cap,
				observedValue: 1
			});
			const gate = parent.gate;
			yield* Effect.acquireRelease(gate.take(1), (permits) => gate.release(permits).pipe(Effect.asVoid), { interruptible: true });
		}),
		retireParent: Effect.fn("SubagentReservations.retireParent")(function* (parentRunId) {
			const result = yield* Ref.modify(state, (ledger) => retireParentTransition(ledger, parentRunId));
			return yield* resolve(result);
		}),
		parentSnapshot: Effect.fn("SubagentReservations.parentSnapshot")(function* (parentRunId) {
			const ledger = yield* Ref.get(state);
			const parent = ledger.parents.get(parentRunId);
			if (parent === void 0) return yield* SubagentParentBudgetUnknown.make({ parentRunId });
			return parentView(ledger, parent);
		})
	});
}));
//#endregion
export { BudgetReservationId, SubagentAvailableAmounts, SubagentBudgetDimension, SubagentBudgetExhausted, SubagentObservedUsage, SubagentParentBudgetActive, SubagentParentBudgetConflict, SubagentParentBudgetUnknown, SubagentParentBudgetView, SubagentReservationConflict, SubagentReservationRequest, SubagentReservationStatus, SubagentReservationUnknown, SubagentReservationView, SubagentReservations, SubagentReservationsMemoryLive, makeBudgetReservationId, SubagentReservations_exports as t };

//# sourceMappingURL=SubagentReservations.mjs.map