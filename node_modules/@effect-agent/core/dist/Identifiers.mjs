import { t as __exportAll } from "./rolldown-runtime-D7D4PA-g.mjs";
import * as Schema from "effect/Schema";
//#region src/Identifiers.ts
var Identifiers_exports = /* @__PURE__ */ __exportAll({
	AgentId: () => AgentId,
	AttemptId: () => AttemptId,
	DelegationId: () => DelegationId,
	EventOffset: () => EventOffset,
	ReceiptId: () => ReceiptId,
	RunId: () => RunId,
	SettlementId: () => SettlementId,
	SubmissionId: () => SubmissionId,
	ThreadId: () => ThreadId,
	ToolCallId: () => ToolCallId,
	TurnId: () => TurnId
});
const identifier = (name) => Schema.NonEmptyString.pipe(Schema.brand(`@effect-agent/core/${name}`));
/** Stable identity of an agent definition, distinct from a display name. */
const AgentId = identifier("AgentId");
/** Identity shared by runs that participate in one thread history. */
const ThreadId = identifier("ThreadId");
/** Identity of one accepted input submission. */
const SubmissionId = identifier("SubmissionId");
/** Durable identity returned once ledger admission and Thread readiness are committed. */
const ReceiptId = identifier("ReceiptId");
/** Identity of the single durable terminal outcome owed to one accepted Submission. */
const SettlementId = identifier("SettlementId");
/** Identity of one execution ownership period for accepted work. */
const AttemptId = identifier("AttemptId");
/** Identity of one logical agent run. */
const RunId = identifier("RunId");
/** Identity of one model turn within a run. */
const TurnId = identifier("TurnId");
/** Identity used to correlate a Tool Call with progress and its terminal outcome. */
const ToolCallId = identifier("ToolCallId");
/** Stable identity of one immutable Delegation Definition. */
const DelegationId = identifier("DelegationId");
/** Opaque position in a semantic event sequence. */
const EventOffset = identifier("EventOffset");
//#endregion
export { AgentId, AttemptId, DelegationId, EventOffset, ReceiptId, RunId, SettlementId, SubmissionId, ThreadId, ToolCallId, TurnId, Identifiers_exports as t };

//# sourceMappingURL=Identifiers.mjs.map