import { t as __exportAll } from "./rolldown-runtime-D7D4PA-g.mjs";
import { AgentId, DelegationId, RunId, ThreadId, ToolCallId, TurnId } from "./Identifiers.mjs";
import { DelegationDepth } from "./SubagentContract.mjs";
import { Schema } from "effect";
//#region src/RunEvent.ts
var RunEvent_exports = /* @__PURE__ */ __exportAll({
	ApprovalRequested: () => ApprovalRequested,
	BudgetWarning: () => BudgetWarning,
	CompactionPerformed: () => CompactionPerformed,
	ExhaustedLimit: () => ExhaustedLimit,
	ModelStarted: () => ModelStarted,
	ReasoningDelta: () => ReasoningDelta,
	RunCompleted: () => RunCompleted,
	RunEvent: () => RunEvent,
	RunFailed: () => RunFailed,
	RunInterrupted: () => RunInterrupted,
	RunStarted: () => RunStarted,
	RunSuspended: () => RunSuspended,
	SubagentCompleted: () => SubagentCompleted,
	SubagentFailed: () => SubagentFailed,
	SubagentInterrupted: () => SubagentInterrupted,
	SubagentJoined: () => SubagentJoined,
	SubagentProgress: () => SubagentProgress,
	SubagentRequested: () => SubagentRequested,
	SubagentStarted: () => SubagentStarted,
	TextDelta: () => TextDelta,
	ToolCallDeclared: () => ToolCallDeclared,
	ToolCallFailed: () => ToolCallFailed,
	ToolCallStarted: () => ToolCallStarted,
	ToolCallSucceeded: () => ToolCallSucceeded,
	ToolProgress: () => ToolProgress,
	TurnCompleted: () => TurnCompleted,
	TurnStarted: () => TurnStarted
});
const RunEventBase = {
	eventVersion: Schema.Literal(1),
	runId: RunId,
	threadId: ThreadId,
	agentId: AgentId,
	sequence: Schema.Natural,
	timestamp: Schema.DateTimeUtcFromString,
	turnId: Schema.optionalKey(TurnId),
	toolCallId: Schema.optionalKey(ToolCallId)
};
/** Signals that the runtime has created a run and assigned its identities. */
var RunStarted = class extends Schema.TaggedClass()("RunStarted", RunEventBase) {};
/** Signals the start of a positive-numbered model turn. */
var TurnStarted = class extends Schema.TaggedClass()("TurnStarted", {
	...RunEventBase,
	turn: Schema.Int.check(Schema.isGreaterThan(0))
}) {};
/** Signals that the model request for a turn is starting. */
var ModelStarted = class extends Schema.TaggedClass()("ModelStarted", {
	...RunEventBase,
	turn: Schema.Int.check(Schema.isGreaterThan(0))
}) {};
/** Carries one live assistant text delta. */
var TextDelta = class extends Schema.TaggedClass()("TextDelta", {
	...RunEventBase,
	text: Schema.String
}) {};
/** Carries reasoning content explicitly exposed by the provider. */
var ReasoningDelta = class extends Schema.TaggedClass()("ReasoningDelta", {
	...RunEventBase,
	text: Schema.String
}) {};
/** Reports a complete model-declared Tool Call and its execution boundary. */
var ToolCallDeclared = class extends Schema.TaggedClass()("ToolCallDeclared", {
	...RunEventBase,
	toolCallId: ToolCallId,
	toolName: Schema.NonEmptyString,
	parameters: Schema.Json,
	providerExecuted: Schema.Boolean
}) {};
/** Signals that a validated Tool Call handler has started. */
var ToolCallStarted = class extends Schema.TaggedClass()("ToolCallStarted", {
	...RunEventBase,
	toolCallId: ToolCallId,
	toolName: Schema.NonEmptyString
}) {};
/** Carries a preliminary Tool result that is not the call's terminal outcome. */
var ToolProgress = class extends Schema.TaggedClass()("ToolProgress", {
	...RunEventBase,
	toolCallId: ToolCallId,
	toolName: Schema.NonEmptyString,
	result: Schema.Json,
	providerExecuted: Schema.Boolean
}) {};
/** Records the successful terminal result of a Tool Call. */
var ToolCallSucceeded = class extends Schema.TaggedClass()("ToolCallSucceeded", {
	...RunEventBase,
	toolCallId: ToolCallId,
	toolName: Schema.NonEmptyString,
	result: Schema.Json,
	providerExecuted: Schema.Boolean
}) {};
/** Records a terminal Tool Call failure using safe, serializable diagnostics. */
var ToolCallFailed = class extends Schema.TaggedClass()("ToolCallFailed", {
	...RunEventBase,
	toolCallId: ToolCallId,
	toolName: Schema.NonEmptyString,
	errorTag: Schema.NonEmptyString,
	message: Schema.String,
	providerExecuted: Schema.Boolean,
	/** Synthetic policy rejection; no handler ran and the failure streak is unchanged. */
	budgetRejected: Schema.optionalKey(Schema.Literal(true))
}) {};
/** Signals that a decoded Tool Call requires approval before execution. */
var ApprovalRequested = class extends Schema.TaggedClass()("ApprovalRequested", {
	...RunEventBase,
	toolCallId: ToolCallId,
	toolName: Schema.NonEmptyString
}) {};
/** Records the normalized finish reason for a completed assistant turn. */
var TurnCompleted = class extends Schema.TaggedClass()("TurnCompleted", {
	...RunEventBase,
	turn: Schema.Int.check(Schema.isGreaterThan(0)),
	finishReason: Schema.Literals([
		"stop",
		"length",
		"content-filter",
		"tool-calls",
		"error",
		"pause",
		"other",
		"unknown"
	])
}) {};
/** Advisory that one policy limit crossed its warning fraction; emitted once per limit per run. */
var BudgetWarning = class extends Schema.TaggedClass()("BudgetWarning", {
	...RunEventBase,
	limit: Schema.Literals([
		"tokens",
		"tool-calls",
		"turns",
		"duration",
		"context"
	]),
	consumed: Schema.Natural,
	limitValue: Schema.Natural
}) {};
/** Records that the engine reduced model context at a Turn seam without erasing source history. */
var CompactionPerformed = class extends Schema.TaggedClass()("CompactionPerformed", {
	...RunEventBase,
	turn: Schema.Int.check(Schema.isGreaterThan(0)),
	kind: Schema.Literals(["clear-tool-results", "summarize"]),
	tokensBeforeEstimate: Schema.Natural,
	tokensAfterEstimate: Schema.Natural
}) {};
/**
* Dimension that bound when a Run settled through the final-answer resolution
* (RUN-018; the token dimension per RUN-025). This is the exhausted marker
* the re-delegation grant flow (RUN-021) consumes and the durable
* `SubmissionSettled` record persists (RUN-011).
*/
const ExhaustedLimit = Schema.Literals([
	"tokens",
	"tool-calls",
	"turns"
]);
const CompletionFinishReason = Schema.Literals([
	"completed",
	"model-stop",
	"budget-exhausted"
]);
const validateCompletionMetadata = ({ exhausted, finishReason }) => {
	return finishReason === "budget-exhausted" === (exhausted !== void 0) ? void 0 : "`exhausted` must be present exactly when `finishReason` is \"budget-exhausted\"";
};
const RunCompletedFields = Schema.Struct({
	...RunEventBase,
	output: Schema.Json,
	/** Schema-encoded application disposition, present only for an ordinary completed Run. */
	runDisposition: Schema.optionalKey(Schema.Json),
	turns: Schema.Int.check(Schema.isGreaterThan(0)),
	finishReason: CompletionFinishReason,
	exhausted: Schema.optionalKey(ExhaustedLimit)
}).check(Schema.makeFilter((event) => validateCompletionMetadata(event)), Schema.makeFilter((event) => event.finishReason === "budget-exhausted" && event.runDisposition !== void 0 ? {
	path: ["runDisposition"],
	issue: "`runDisposition` is not allowed when `finishReason` is \"budget-exhausted\""
} : void 0));
/**
* Successful terminal event carrying Schema-compatible output and the completed turn count.
* `"budget-exhausted"` marks a Run that settled through the policy's final-answer resolution
* after Turn, Tool Call, or token exhaustion — never a plain `"model-stop"` (RUN-011); its
* `turns` count may exceed `maxTurns` by the single grace Turn, and `exhausted` names the
* dimension that bound.
*
* Invariant: `exhausted` is present exactly when `finishReason` is
* `"budget-exhausted"`, and a budget-exhausted completion has no application
* run disposition. The checked Struct enforces both rules during construction,
* decoding, and encoding without replacing the class in `RunEvent`.
*/
var RunCompleted = class extends Schema.TaggedClass()("RunCompleted", RunCompletedFields) {};
/** Terminal event for a run that failed with an expected error. */
var RunFailed = class extends Schema.TaggedClass()("RunFailed", {
	...RunEventBase,
	errorTag: Schema.NonEmptyString,
	message: Schema.String
}) {};
/** Terminal event for a run stopped by interruption. */
var RunInterrupted = class extends Schema.TaggedClass()("RunInterrupted", {
	...RunEventBase,
	message: Schema.String
}) {};
/** Terminal event for a run awaiting resumable external input. */
var RunSuspended = class extends Schema.TaggedClass()("RunSuspended", {
	...RunEventBase,
	reason: Schema.String
}) {};
const SubagentText = Schema.String.check(Schema.isMaxLength(4096));
const SubagentEventBase = {
	...RunEventBase,
	turnId: TurnId,
	toolCallId: ToolCallId,
	delegationId: DelegationId,
	childThreadId: ThreadId,
	childRunId: RunId,
	targetAgentId: AgentId,
	depth: DelegationDepth
};
/** Signals that a delegation Tool Call passed preflight with preallocated child identity. */
var SubagentRequested = class extends Schema.TaggedClass()("SubagentRequested", SubagentEventBase) {};
/** Signals that the child run has started inside its parent-owned scoped region. */
var SubagentStarted = class extends Schema.TaggedClass()("SubagentStarted", SubagentEventBase) {};
/** Carries a bounded child progress summary without duplicating the child event stream. */
var SubagentProgress = class extends Schema.TaggedClass()("SubagentProgress", {
	...SubagentEventBase,
	summary: SubagentText
}) {};
/**
* Records the child run's successful terminal outcome before the parent join.
* Same invariant as `RunCompleted`: `exhausted` is present exactly when
* `finishReason` is `"budget-exhausted"` (the pair travels verbatim from the
* child's terminal event); consumers treat a divergent pair fail-safe.
*/
var SubagentCompleted = class extends Schema.TaggedClass()("SubagentCompleted", Schema.Struct({
	...SubagentEventBase,
	turns: Schema.Int.check(Schema.isGreaterThan(0)),
	finishReason: CompletionFinishReason,
	exhausted: Schema.optionalKey(ExhaustedLimit)
}).check(Schema.makeFilter((event) => validateCompletionMetadata(event)))) {};
/** Records the child run's expected terminal failure using safe, serializable diagnostics. */
var SubagentFailed = class extends Schema.TaggedClass()("SubagentFailed", {
	...SubagentEventBase,
	errorTag: Schema.NonEmptyString,
	message: SubagentText
}) {};
/** Records that the child run was stopped by interruption before a terminal outcome. */
var SubagentInterrupted = class extends Schema.TaggedClass()("SubagentInterrupted", {
	...SubagentEventBase,
	reason: SubagentText
}) {};
/** Records that the child's terminal outcome was joined into its parent delegation Tool Call. */
var SubagentJoined = class extends Schema.TaggedClass()("SubagentJoined", SubagentEventBase) {};
/** Versioned union of stable semantic run events, excluding raw provider chunks. */
const RunEvent = Schema.Union([
	RunStarted,
	TurnStarted,
	ModelStarted,
	TextDelta,
	ReasoningDelta,
	ToolCallDeclared,
	ToolCallStarted,
	ToolProgress,
	ToolCallSucceeded,
	ToolCallFailed,
	ApprovalRequested,
	TurnCompleted,
	BudgetWarning,
	CompactionPerformed,
	RunCompleted,
	RunFailed,
	RunInterrupted,
	RunSuspended,
	SubagentRequested,
	SubagentStarted,
	SubagentProgress,
	SubagentCompleted,
	SubagentFailed,
	SubagentInterrupted,
	SubagentJoined
]);
//#endregion
export { ApprovalRequested, BudgetWarning, CompactionPerformed, ExhaustedLimit, ModelStarted, ReasoningDelta, RunCompleted, RunEvent, RunFailed, RunInterrupted, RunStarted, RunSuspended, SubagentCompleted, SubagentFailed, SubagentInterrupted, SubagentJoined, SubagentProgress, SubagentRequested, SubagentStarted, TextDelta, ToolCallDeclared, ToolCallFailed, ToolCallStarted, ToolCallSucceeded, ToolProgress, TurnCompleted, TurnStarted, RunEvent_exports as t };

//# sourceMappingURL=RunEvent.mjs.map