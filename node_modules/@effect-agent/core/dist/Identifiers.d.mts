import * as Schema from "effect/Schema";
declare namespace Identifiers_d_exports {
  export { AgentId, AttemptId, DelegationId, EventOffset, ReceiptId, RunId, SettlementId, SubmissionId, ThreadId, ToolCallId, TurnId };
}
/** Stable identity of an agent definition, distinct from a display name. */
declare const AgentId: Schema.brand<Schema.NonEmptyString, "@effect-agent/core/AgentId">;
type AgentId = typeof AgentId.Type;
/** Identity shared by runs that participate in one thread history. */
declare const ThreadId: Schema.brand<Schema.NonEmptyString, "@effect-agent/core/ThreadId">;
type ThreadId = typeof ThreadId.Type;
/** Identity of one accepted input submission. */
declare const SubmissionId: Schema.brand<Schema.NonEmptyString, "@effect-agent/core/SubmissionId">;
type SubmissionId = typeof SubmissionId.Type;
/** Durable identity returned once ledger admission and Thread readiness are committed. */
declare const ReceiptId: Schema.brand<Schema.NonEmptyString, "@effect-agent/core/ReceiptId">;
type ReceiptId = typeof ReceiptId.Type;
/** Identity of the single durable terminal outcome owed to one accepted Submission. */
declare const SettlementId: Schema.brand<Schema.NonEmptyString, "@effect-agent/core/SettlementId">;
type SettlementId = typeof SettlementId.Type;
/** Identity of one execution ownership period for accepted work. */
declare const AttemptId: Schema.brand<Schema.NonEmptyString, "@effect-agent/core/AttemptId">;
type AttemptId = typeof AttemptId.Type;
/** Identity of one logical agent run. */
declare const RunId: Schema.brand<Schema.NonEmptyString, "@effect-agent/core/RunId">;
type RunId = typeof RunId.Type;
/** Identity of one model turn within a run. */
declare const TurnId: Schema.brand<Schema.NonEmptyString, "@effect-agent/core/TurnId">;
type TurnId = typeof TurnId.Type;
/** Identity used to correlate a Tool Call with progress and its terminal outcome. */
declare const ToolCallId: Schema.brand<Schema.NonEmptyString, "@effect-agent/core/ToolCallId">;
type ToolCallId = typeof ToolCallId.Type;
/** Stable identity of one immutable Delegation Definition. */
declare const DelegationId: Schema.brand<Schema.NonEmptyString, "@effect-agent/core/DelegationId">;
type DelegationId = typeof DelegationId.Type;
/** Opaque position in a semantic event sequence. */
declare const EventOffset: Schema.brand<Schema.NonEmptyString, "@effect-agent/core/EventOffset">;
type EventOffset = typeof EventOffset.Type;
//#endregion
export { AgentId, AttemptId, DelegationId, EventOffset, ReceiptId, RunId, SettlementId, SubmissionId, ThreadId, ToolCallId, TurnId, Identifiers_d_exports as t };
//# sourceMappingURL=Identifiers.d.mts.map