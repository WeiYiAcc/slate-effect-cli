import { Schema } from "effect";
declare namespace RunPolicyUsage_d_exports {
  export { RunPolicyUsage };
}
/**
 * Cumulative policy accounting for one logical Run across replacement Attempts.
 * Turns and model-declared calls include a pending batch. The failure streak excludes that
 * entire batch until its declaration-ordered outcomes are folded once during continuation.
 * Programmatic calls are reserved before execution; finalization is reserved before its model call.
 */
declare const RunPolicyUsage: Schema.Struct<{
  readonly committedTurns: Schema.Natural;
  readonly toolCalls: Schema.Natural;
  readonly programmaticToolCalls: Schema.Natural;
  readonly consecutiveToolFailures: Schema.Natural;
  readonly finalizationUsed: Schema.Boolean;
}>;
type RunPolicyUsage = typeof RunPolicyUsage.Type;
//#endregion
export { RunPolicyUsage, RunPolicyUsage_d_exports as t };
//# sourceMappingURL=RunPolicyUsage.d.mts.map