import { t as __exportAll } from "./rolldown-runtime-D7D4PA-g.mjs";
import { AgentPolicy } from "./AgentPolicy.mjs";
import { AgentId } from "./Identifiers.mjs";
import * as S from "effect/Schema";
//#region src/Agent.ts
var Agent_exports = /* @__PURE__ */ __exportAll({
	make: () => make,
	withModel: () => withModel
});
function make(id, options) {
	return Object.freeze({
		...options,
		policy: AgentPolicy.resolve(options.policy),
		policyOverrides: Object.freeze({ ...options.policy }),
		id: S.decodeSync(AgentId)(id),
		metadata: options.metadata === void 0 ? void 0 : Object.freeze({ ...options.metadata }),
		completion: options.completion === void 0 ? void 0 : Object.freeze({ ...options.completion }),
		runDisposition: options.runDisposition === void 0 ? void 0 : Object.freeze({ ...options.runDisposition })
	});
}
/** Fix a model Layer for registration or delegation without acquiring or hiding its requirements. */
const withModel = (definition, model) => Object.freeze({
	definition,
	model
});
//#endregion
export { make, Agent_exports as t, withModel };

//# sourceMappingURL=Agent.mjs.map