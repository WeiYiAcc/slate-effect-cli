import { t as __exportAll } from "./rolldown-runtime-D7D4PA-g.mjs";
import { MemorySourceReference } from "./MemoryReference.mjs";
import { MemoryDocument, MemoryKey, MemoryWrite } from "./MemoryStore.mjs";
import { Context, Effect, Layer, Schema } from "effect";
//#region src/RememberingStore.ts
var RememberingStore_exports = /* @__PURE__ */ __exportAll({
	Admission: () => Admission,
	AdmissionError: () => AdmissionError,
	Checkpoint: () => Checkpoint,
	CheckpointError: () => CheckpointError,
	Evidence: () => Evidence,
	Intent: () => Intent,
	Invalidation: () => Invalidation,
	InvalidationReceipt: () => InvalidationReceipt,
	MutationFailpoint: () => MutationFailpoint,
	MutationFailure: () => MutationFailure,
	MutationPoint: () => MutationPoint,
	ProcessingError: () => ProcessingError,
	Progress: () => Progress,
	Proposal: () => Proposal,
	Source: () => Source,
	SourcePosition: () => SourcePosition,
	comparePosition: () => comparePosition
});
const Identity = Schema.NonEmptyString.check(Schema.isMaxLength(256));
const Counter = Schema.Int.check(Schema.isBetween({
	minimum: 0,
	maximum: Number.MAX_SAFE_INTEGER
}));
/** Host source-order lineage. Sequence is monotonic only within one authorityGeneration.
* Opaque revision strings and different authority generations are never ordered.
*/
const SourcePosition = Schema.Struct({
	authorityGeneration: Identity,
	sequence: Counter
});
const comparePosition = (left, right) => left.authorityGeneration === right.authorityGeneration ? Math.sign(left.sequence - right.sequence) : void 0;
const Source = Schema.Struct({
	key: MemoryKey.Wire,
	locator: MemorySourceReference.fields.locator,
	revision: Schema.NonEmptyString.check(Schema.isMaxLength(1024)),
	position: SourcePosition
});
var IntentWire = class extends Schema.Class("@effect-agent/remembering/Intent")({
	version: Schema.Literal(1),
	/** Stable host-selected idempotency identity, including processor/schema version. It must be
	* unique across all source-owner Stores writing this target for the receipt retention window.
	*/
	id: Identity,
	/** Trusted invocation identity, never taken from extracted/model values. */
	invocationId: Identity,
	source: Source,
	target: MemoryKey.Wire
}) {};
const Intent = {
	Wire: IntentWire,
	make: (fields) => Object.assign(Schema.decodeUnknownSync(IntentWire)(fields), {
		source: {
			...fields.source,
			key: MemoryKey.make(fields.source.key)
		},
		target: MemoryKey.make(fields.target)
	})
};
/** Half-open UTF-8 byte range in the exact authoritative source revision. A quote proves
* provenance only. Applications decide whether it supports a fact and may be shared.
*/
const Evidence = Schema.Struct({
	source: Schema.Struct({
		...MemorySourceReference.fields,
		revision: Source.fields.revision
	}),
	startByte: Counter,
	endByte: Counter,
	quote: Schema.NonEmptyString.check(Schema.isMaxLength(65536))
});
const Proposal = Schema.Struct({
	version: Schema.Literal(1),
	value: Schema.Json,
	evidence: Schema.Array(Evidence).check(Schema.isMinLength(1), Schema.isMaxLength(128))
});
var Invalidation = class extends Schema.Class("@effect-agent/remembering/Invalidation")({
	version: Schema.Literal(1),
	id: Identity,
	source: MemoryKey.Wire,
	position: SourcePosition,
	reason: Schema.Literals([
		"source-edit",
		"source-deletion",
		"source-revocation",
		"forget"
	])
}) {};
const PreparedFields = {
	proposal: Proposal,
	attempt: Counter,
	purpose: Schema.Literals(["remember", "cleanup"]),
	/** Original applied revision, retained for source-aware conditional cleanup. */
	applied: Schema.NullOr(MemoryDocument.Wire)
};
const CompletedFields = {
	proposal: Schema.NullOr(Proposal),
	attempt: Counter,
	applied: Schema.NullOr(MemoryDocument.Wire),
	outcome: Schema.Literals([
		"applied",
		"no-change",
		"rejected",
		"suppressed"
	]),
	cleaned: Schema.Boolean
};
const Progress = Schema.Union([
	Schema.TaggedStruct("Pending", {}),
	Schema.TaggedStruct("Proposed", PreparedFields),
	Schema.TaggedStruct("Prepared", {
		...PreparedFields,
		command: MemoryWrite.Wire
	}),
	Schema.TaggedStruct("Completed", CompletedFields)
]);
/** Retain this source-target reference after removing a job from the active queue. It contains
* the immutable proposal and original application receipt needed by later invalidation.
* Prepared commands must never be pruned or replaced while their outcome is uncertain.
*/
var Checkpoint = class extends Schema.Class("@effect-agent/remembering/Checkpoint")({
	version: Counter,
	intent: Intent.Wire,
	suppression: Schema.NullOr(Invalidation),
	progress: Progress
}) {};
var Admission = class extends Schema.Class("@effect-agent/remembering/Admission")({
	id: Identity,
	status: Schema.Literals(["queued", "duplicate"])
}) {};
var InvalidationReceipt = class extends Schema.Class("@effect-agent/remembering/InvalidationReceipt")({
	id: Identity,
	status: Schema.Literals([
		"accepted",
		"duplicate",
		"stale"
	]),
	affected: Counter
}) {};
var AdmissionError = class extends Schema.TaggedError()("RememberingAdmissionError", { reason: Schema.Literals([
	"invalid-input",
	"conflict",
	"capacity",
	"suppressed",
	"disabled"
]) }) {};
var CheckpointError = class extends Schema.TaggedError()("RememberingCheckpointError", { reason: Schema.Literals([
	"fenced",
	"missing",
	"corrupt"
]) }) {};
var ProcessingError = class extends Schema.TaggedError()("RememberingProcessingError", { reason: Schema.Literals([
	"invalid-input",
	"invalid-evidence",
	"source-changed",
	"budget",
	"timeout",
	"disabled",
	"cleanup-rejected"
]) }) {};
const MutationPoint = Schema.Literals([
	"admission:before",
	"admission:after",
	"proposal:before",
	"proposal:after",
	"command:before",
	"command:after",
	"rebase:before",
	"rebase:after",
	"completion:before",
	"completion:after",
	"suppression:before",
	"suppression:after",
	"cleanup:before",
	"cleanup:after"
]);
var MutationFailure = class extends Schema.TaggedError()("RememberingMutationFailure", { point: MutationPoint }) {};
/** Explicit transition failpoints. No-op unless a test installs a replacement. */
var MutationFailpoint = class extends Context.Service()("@effect-agent/remembering/MutationFailpoint") {
	static layer = Layer.succeed(this, { hit: () => Effect.void });
};
//#endregion
export { Admission, AdmissionError, Checkpoint, CheckpointError, Evidence, Intent, Invalidation, InvalidationReceipt, MutationFailpoint, MutationFailure, MutationPoint, ProcessingError, Progress, Proposal, Source, SourcePosition, comparePosition, RememberingStore_exports as t };

//# sourceMappingURL=RememberingStore.mjs.map