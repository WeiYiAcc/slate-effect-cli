import { t as __exportAll } from "./rolldown-runtime-D7D4PA-g.mjs";
import { SANDBOX_DIAGNOSTIC_MAX_LENGTH, SandboxImplementation, SandboxNetworkPolicy } from "./Sandbox.mjs";
import { Context, Duration, Schema } from "effect";
//#region src/CodeExecutor.ts
var CodeExecutor_exports = /* @__PURE__ */ __exportAll({
	CodeExecutionError: () => CodeExecutionError,
	CodeExecutionHost: () => CodeExecutionHost,
	CodeExecutionLimits: () => CodeExecutionLimits,
	CodeExecutionNamespace: () => CodeExecutionNamespace,
	CodeExecutionProtocolError: () => CodeExecutionProtocolError,
	CodeExecutionRequest: () => CodeExecutionRequest,
	CodeExecutionResourceUse: () => CodeExecutionResourceUse,
	CodeExecutionResult: () => CodeExecutionResult,
	CodeExecutionTimeoutError: () => CodeExecutionTimeoutError,
	CodeExecutor: () => CodeExecutor,
	CodeExecutorStartError: () => CodeExecutorStartError,
	CodeExecutorTerminatedError: () => CodeExecutorTerminatedError,
	CodeExecutorUnsupportedError: () => CodeExecutorUnsupportedError,
	CodeHostCall: () => CodeHostCall,
	CodeHostCallFailure: () => CodeHostCallFailure,
	CodeHostCallLimitError: () => CodeHostCallLimitError,
	CodeHostCallResult: () => CodeHostCallResult,
	CodeHostCallSuccess: () => CodeHostCallSuccess,
	CodeOutputLimitError: () => CodeOutputLimitError,
	CodeProgramFailedError: () => CodeProgramFailedError,
	CodeSourceError: () => CodeSourceError,
	JsIdentifier: () => JsIdentifier
});
const BoundedLogLine = Schema.String.check(Schema.isMaxLength(16384));
const BoundedMessage = Schema.String.check(Schema.isMaxLength(SANDBOX_DIAGNOSTIC_MAX_LENGTH));
const BoundedLogs = Schema.Array(BoundedLogLine).check(Schema.isMaxLength(4096));
const BoundedSourceText = Schema.String.check(Schema.isMaxLength(4194304));
const PositiveInt = Schema.Int.check(Schema.isGreaterThan(0));
const FinitePositiveDuration = Schema.Duration.pipe(Schema.refine((duration) => Duration.isFinite(duration) && Duration.isPositive(duration), { expected: "a finite positive duration" }));
const FiniteNonNegativeDuration = Schema.Duration.pipe(Schema.refine((duration) => Duration.isFinite(duration) && !Duration.isNegative(duration), { expected: "a finite non-negative duration" }));
/**
* ECMAScript reserved words are rejected up front: a namespace or method that
* needs one would make every harness that binds identifiers fail at runtime
* instead of failing closed at the Schema boundary.
*/
const reservedIdentifiers = /* @__PURE__ */ new Set([
	"arguments",
	"await",
	"break",
	"case",
	"catch",
	"class",
	"const",
	"continue",
	"debugger",
	"default",
	"delete",
	"do",
	"else",
	"enum",
	"eval",
	"export",
	"extends",
	"false",
	"finally",
	"for",
	"function",
	"if",
	"implements",
	"import",
	"in",
	"instanceof",
	"interface",
	"let",
	"new",
	"null",
	"package",
	"private",
	"protected",
	"public",
	"return",
	"static",
	"super",
	"switch",
	"this",
	"throw",
	"true",
	"try",
	"typeof",
	"var",
	"void",
	"while",
	"with",
	"yield"
]);
/** A JavaScript identifier safe to bind as a sandbox global or method name. */
const JsIdentifier = Schema.NonEmptyString.check(Schema.isMaxLength(128), Schema.isPattern(/^[A-Za-z_$][A-Za-z0-9_$]*$/)).pipe(Schema.refine((value) => !reservedIdentifiers.has(value), { expected: "a JavaScript identifier that is not a reserved word" }));
/** One callable namespace exposed to the generated program as a typed global. */
var CodeExecutionNamespace = class extends Schema.Class("CodeExecutionNamespace")({
	name: JsIdentifier,
	methods: Schema.Array(JsIdentifier).check(Schema.isMaxLength(64), Schema.isMinLength(1))
}) {};
/**
* Limits an executor must either enforce or reject (CAP-015). Byte bounds are
* measured on UTF-8 encoded JSON at the sandbox boundary.
*/
var CodeExecutionLimits = class extends Schema.Class("CodeExecutionLimits")({
	maxSourceBytes: PositiveInt.check(Schema.isLessThanOrEqualTo(4194304)),
	maxWallTime: FinitePositiveDuration,
	cpuMillis: Schema.optionalKey(PositiveInt.check(Schema.isLessThanOrEqualTo(3e5))),
	maxLogBytes: PositiveInt.check(Schema.isLessThanOrEqualTo(1048576)),
	maxResultBytes: PositiveInt.check(Schema.isLessThanOrEqualTo(4194304)),
	maxHostCalls: Schema.Natural.check(Schema.isLessThanOrEqualTo(1e4)),
	maxHostCallArgumentBytes: PositiveInt.check(Schema.isLessThanOrEqualTo(1048576)),
	maxHostCallResultBytes: PositiveInt.check(Schema.isLessThanOrEqualTo(4194304))
}) {};
/**
* One isolated execution pass over bounded generated JavaScript. The source
* must be a single expression evaluating to one async function; the executor
* harness invokes it exactly once with no arguments (ADR-0017 decision 5).
*/
var CodeExecutionRequest = class extends Schema.Class("CodeExecutionRequest")({
	language: Schema.Literals(["javascript"]),
	source: BoundedSourceText,
	namespaces: Schema.Array(CodeExecutionNamespace).check(Schema.isMaxLength(32)),
	network: SandboxNetworkPolicy,
	limits: CodeExecutionLimits
}) {};
/** One host invocation from generated code. Data only: the broker owns identity. */
var CodeHostCall = class extends Schema.Class("CodeHostCall")({
	namespace: JsIdentifier,
	method: JsIdentifier,
	argument: Schema.Json
}) {};
/** A host call settled with the encoded Tool success value. */
var CodeHostCallSuccess = class extends Schema.TaggedClass()("CodeHostCallSuccess", { value: Schema.Json }) {};
/**
* A host call settled with a Schema-encoded typed failure envelope. Inside the
* generated program the call's Promise rejects with the envelope value, so a
* program can catch and branch on expected failures.
*/
var CodeHostCallFailure = class extends Schema.TaggedClass()("CodeHostCallFailure", { error: Schema.Json }) {};
/** Outcome of one host call as data; transport failures stay executor errors. */
const CodeHostCallResult = Schema.Union([CodeHostCallSuccess, CodeHostCallFailure]);
/**
* The per-pass host capability: the only authority generated code has over the
* host. It is provided at the pass edge by the capability layer, its live
* bindings are scoped resources, and it is never persisted (ADR-0017
* decision 2). Broker-level failures return as `CodeHostCallFailure` data;
* an unexpected host defect remains a defect.
*/
var CodeExecutionHost = class extends Context.Service()("@effect-agent/sandbox/CodeExecutionHost") {};
/** Accounting one executor observed for one pass. */
var CodeExecutionResourceUse = class extends Schema.Class("CodeExecutionResourceUse")({
	wallTime: FiniteNonNegativeDuration,
	hostCalls: Schema.Natural,
	logBytes: Schema.Natural,
	resultBytes: Schema.Natural,
	cpuMillis: Schema.optionalKey(Schema.Natural)
}) {};
/**
* The bounded outcome of one successful pass. `value` and `logs` are the raw
* executor capture; the aggregate model-visible egress budget and redaction
* pass are owned by the Code Mode capability, not the executor (CAP-016).
*/
var CodeExecutionResult = class extends Schema.Class("CodeExecutionResult")({
	implementation: SandboxImplementation,
	value: Schema.Json,
	logs: BoundedLogs,
	resourceUse: CodeExecutionResourceUse
}) {};
/** The source is not one async function expression the executor can run. */
var CodeSourceError = class extends Schema.TaggedError()("CodeSourceError", {
	implementation: SandboxImplementation,
	reason: Schema.Literals([
		"invalid",
		"oversized",
		"not-a-function"
	]),
	message: BoundedMessage
}) {};
/** The executor cannot honestly enforce one requested feature, limit, or policy. */
var CodeExecutorUnsupportedError = class extends Schema.TaggedError()("CodeExecutorUnsupportedError", {
	implementation: SandboxImplementation,
	feature: Schema.Literals([
		"language",
		"network",
		"cpu-limit",
		"namespaces",
		"wall-clock",
		"host-calls"
	]),
	message: BoundedMessage
}) {};
/** The executor could not start the isolated pass. */
var CodeExecutorStartError = class extends Schema.TaggedError()("CodeExecutorStartError", {
	implementation: SandboxImplementation,
	message: BoundedMessage,
	cause: Schema.optionalKey(Schema.Defect())
}) {};
/** The pass exceeded its CPU or wall-clock budget and was terminated. */
var CodeExecutionTimeoutError = class extends Schema.TaggedError()("CodeExecutionTimeoutError", {
	implementation: SandboxImplementation,
	kind: Schema.Literals(["cpu", "wall-clock"]),
	maxWallTime: FinitePositiveDuration,
	logs: BoundedLogs
}) {};
/** A bounded surface (logs, result, or a per-call byte bound) was exceeded. */
var CodeOutputLimitError = class extends Schema.TaggedError()("CodeOutputLimitError", {
	implementation: SandboxImplementation,
	surface: Schema.Literals([
		"logs",
		"result",
		"host-call-argument",
		"host-call-result"
	]),
	limit: PositiveInt,
	observed: Schema.Natural,
	logs: BoundedLogs
}) {};
/** The pass issued more host calls than its executor-owned cap allows. */
var CodeHostCallLimitError = class extends Schema.TaggedError()("CodeHostCallLimitError", {
	implementation: SandboxImplementation,
	limit: Schema.Natural,
	logs: BoundedLogs
}) {};
/**
* The generated program finished by failing: it threw, its promise rejected,
* or it returned a value that is not JSON. The bounded thrown value and log
* capture flow back so a model can correct the program without a blind retry.
*/
var CodeProgramFailedError = class extends Schema.TaggedError()("CodeProgramFailedError", {
	implementation: SandboxImplementation,
	reason: Schema.Literals([
		"threw",
		"rejected",
		"non-json-result"
	]),
	thrown: Schema.Json,
	message: BoundedMessage,
	logs: BoundedLogs
}) {};
/** The executor or its transport produced a value outside the protocol Schemas. */
var CodeExecutionProtocolError = class extends Schema.TaggedError()("CodeExecutionProtocolError", {
	implementation: SandboxImplementation,
	message: BoundedMessage
}) {};
/**
* The pass was terminated externally — isolate eviction or a platform kill —
* with no terminal program result. No result is fabricated.
*/
var CodeExecutorTerminatedError = class extends Schema.TaggedError()("CodeExecutorTerminatedError", {
	implementation: SandboxImplementation,
	message: BoundedMessage
}) {};
/** Expected Code Mode execution failures. Interruption stays an Effect interruption. */
const CodeExecutionError = Schema.Union([
	CodeSourceError,
	CodeExecutorUnsupportedError,
	CodeExecutorStartError,
	CodeExecutionTimeoutError,
	CodeOutputLimitError,
	CodeHostCallLimitError,
	CodeProgramFailedError,
	CodeExecutionProtocolError,
	CodeExecutorTerminatedError
]);
/**
* Callback-capable isolated execution port for Code Mode (ADR-0017), a sibling
* of the command-shaped `Sandbox` service. One `execute` is one stateless pass:
* the executor owns no approval, Tool, replay, or Thread semantics, holds
* every pass resource in the given Scope, and reaches the host only through the
* `CodeExecutionHost` service in its requirement channel. Implementations must
* identify their isolation posture honestly; an `unisolated` executor is never
* a security boundary (CAP-010, CAP-015).
*/
var CodeExecutor = class extends Context.Service()("@effect-agent/sandbox/CodeExecutor") {};
//#endregion
export { CodeExecutionError, CodeExecutionHost, CodeExecutionLimits, CodeExecutionNamespace, CodeExecutionProtocolError, CodeExecutionRequest, CodeExecutionResourceUse, CodeExecutionResult, CodeExecutionTimeoutError, CodeExecutor, CodeExecutorStartError, CodeExecutorTerminatedError, CodeExecutorUnsupportedError, CodeHostCall, CodeHostCallFailure, CodeHostCallLimitError, CodeHostCallResult, CodeHostCallSuccess, CodeOutputLimitError, CodeProgramFailedError, CodeSourceError, JsIdentifier, CodeExecutor_exports as t };

//# sourceMappingURL=CodeExecutor.mjs.map