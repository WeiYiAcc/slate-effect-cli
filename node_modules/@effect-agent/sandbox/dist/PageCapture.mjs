import { t as __exportAll } from "./rolldown-runtime-D7D4PA-g.mjs";
import { SANDBOX_DIAGNOSTIC_MAX_LENGTH, SandboxImplementation } from "./Sandbox.mjs";
import { Context, Encoding, Predicate, Schema } from "effect";
//#region src/PageCapture.ts
var PageCapture_exports = /* @__PURE__ */ __exportAll({
	CapturePageContent: () => CapturePageContent,
	CapturePageLinks: () => CapturePageLinks,
	CapturePageMarkdown: () => CapturePageMarkdown,
	CapturePageScrape: () => CapturePageScrape,
	CapturePageStructured: () => CapturePageStructured,
	PageCapture: () => PageCapture,
	PageCaptureAction: () => PageCaptureAction,
	PageCaptureEngine: () => PageCaptureEngine,
	PageCaptureError: () => PageCaptureError,
	PageCaptureInferencePolicyError: () => PageCaptureInferencePolicyError,
	PageCaptureInferenceUse: () => PageCaptureInferenceUse,
	PageCaptureLimits: () => PageCaptureLimits,
	PageCaptureLinkUrl: () => PageCaptureLinkUrl,
	PageCaptureNavigationError: () => PageCaptureNavigationError,
	PageCaptureOutput: () => PageCaptureOutput,
	PageCaptureOutputLimitError: () => PageCaptureOutputLimitError,
	PageCaptureProtocolError: () => PageCaptureProtocolError,
	PageCaptureRateLimitedError: () => PageCaptureRateLimitedError,
	PageCaptureRequest: () => PageCaptureRequest,
	PageCaptureResourceUse: () => PageCaptureResourceUse,
	PageCaptureResponseFormat: () => PageCaptureResponseFormat,
	PageCaptureResult: () => PageCaptureResult,
	PageCaptureTarget: () => PageCaptureTarget,
	PageCaptureTargetUrl: () => PageCaptureTargetUrl,
	PageCaptureUnsupportedError: () => PageCaptureUnsupportedError,
	PageContentCaptured: () => PageContentCaptured,
	PageHtmlTarget: () => PageHtmlTarget,
	PageLinksCaptured: () => PageLinksCaptured,
	PageMarkdownCaptured: () => PageMarkdownCaptured,
	PageNavigationOptions: () => PageNavigationOptions,
	PageResourcePolicy: () => PageResourcePolicy,
	PageScrapeAttribute: () => PageScrapeAttribute,
	PageScrapeCaptured: () => PageScrapeCaptured,
	PageScrapeElement: () => PageScrapeElement,
	PageScrapeGroup: () => PageScrapeGroup,
	PageSelectorWait: () => PageSelectorWait,
	PageStructuredCaptured: () => PageStructuredCaptured,
	PageUrlTarget: () => PageUrlTarget,
	PageViewport: () => PageViewport
});
/**
* Page capture (capability spec §9.2): one stateless, schema-first rendering
* pass over a remote or supplied page — navigate (or load raw HTML), render
* with JavaScript, and return exactly one bounded output. It is a sibling of
* the command-shaped `Sandbox` and callback-shaped `CodeExecutor` ports: a
* browser is intrinsically an egress device, so it carries its own explicit
* capture contract instead of widening the sandbox network policy that every
* existing adapter rejects.
*
* Everything a capture returns is untrusted, attacker-influenced content
* (security spec §9). Stateful browser sessions, screenshots, PDFs, snapshot
* bundles, crawling, and accessibility trees are deliberately outside this
* port's first slice.
*/
const MAX_OUTPUT_BYTES = 8388608;
const MAX_LINKS = 4096;
const MAX_SCRAPE_SELECTORS = 64;
const MAX_SCRAPE_ELEMENTS = 4096;
const MAX_SCRAPE_ATTRIBUTES = 64;
const MAX_RESPONSE_FORMAT_BYTES = 65536;
const MAX_RESPONSE_FORMAT_DEPTH = 32;
const MAX_RESPONSE_FORMAT_NODES = 4096;
const MAX_RESPONSE_FORMAT_COLLECTION_LENGTH = 256;
const BoundedOutputText = Schema.String.check(Schema.isMaxLength(MAX_OUTPUT_BYTES));
const BoundedUrl = Schema.NonEmptyString.check(Schema.isMaxLength(8192));
const BoundedHtml = Schema.NonEmptyString.check(Schema.isMaxLength(2097152));
const BoundedPrompt = Schema.NonEmptyString.check(Schema.isMaxLength(8192));
const BoundedSelector = Schema.NonEmptyString.check(Schema.isMaxLength(1024));
const BoundedAttributeName = Schema.NonEmptyString.check(Schema.isMaxLength(1024));
const BoundedAttributeValue = Schema.String.check(Schema.isMaxLength(65536));
const BoundedScrapeText = Schema.String.check(Schema.isMaxLength(1048576));
const BoundedScrapeHtml = Schema.String.check(Schema.isMaxLength(2097152));
const BoundedPattern = Schema.NonEmptyString.check(Schema.isMaxLength(1024));
const BoundedMessage = Schema.String.check(Schema.isMaxLength(SANDBOX_DIAGNOSTIC_MAX_LENGTH));
const BoundedInferenceProvider = Schema.NonEmptyString.check(Schema.isMaxLength(256));
const BoundedSchemaProperty = Schema.NonEmptyString.check(Schema.isMaxLength(256));
const BoundedSchemaText = Schema.String.check(Schema.isMaxLength(8192));
const BoundedSchemaReference = Schema.NonEmptyString.check(Schema.isMaxLength(8192));
const PositiveInt = Schema.Int.check(Schema.isGreaterThan(0));
/** Bounded by the platform's 60-second browser inactivity ceiling. */
const BoundedTimeoutMillis = PositiveInt.check(Schema.isLessThanOrEqualTo(6e4));
const pageUrlConstructor = Reflect.get(globalThis, "URL");
const isCredentialFreeWebUrl = (value, allowHttp) => {
	if (typeof pageUrlConstructor !== "function") return false;
	try {
		const parsed = Reflect.construct(pageUrlConstructor, [value]);
		if (!Predicate.isObject(parsed)) return false;
		const protocol = Reflect.get(parsed, "protocol");
		const hostname = Reflect.get(parsed, "hostname");
		const username = Reflect.get(parsed, "username");
		const password = Reflect.get(parsed, "password");
		return (protocol === "https:" || allowHttp && protocol === "http:") && typeof hostname === "string" && hostname.length > 0 && username === "" && password === "";
	} catch {
		return false;
	}
};
/** Absolute, bounded HTTPS navigation URL that never carries embedded credentials. */
const PageCaptureTargetUrl = BoundedUrl.check(Schema.makeFilter((value) => isCredentialFreeWebUrl(value, false), { title: "an absolute HTTPS URL without embedded credentials" }));
/** Discovered HTTP(S) links are data, never navigation authority, and carry no credentials. */
const PageCaptureLinkUrl = BoundedUrl.check(Schema.makeFilter((value) => isCredentialFreeWebUrl(value, true), { title: "an absolute HTTP or HTTPS URL without embedded credentials" }));
const ResponseFormatPrimitive = Schema.Literals([
	"string",
	"number",
	"boolean",
	"array",
	"object",
	"null",
	"integer"
]);
const ResponseFormatPrimitiveList = Schema.Array(ResponseFormatPrimitive).check(Schema.isMinLength(1), Schema.isMaxLength(7), Schema.isUnique());
const ResponseFormatNode = Schema.suspend(() => Schema.Struct(responseFormatFields()).pipe(Schema.annotate({ parseOptions: { onExcessProperty: "error" } })));
const responseFormatFields = () => {
	const boundedNodes = Schema.Array(ResponseFormatNode).check(Schema.isMinLength(1), Schema.isMaxLength(MAX_RESPONSE_FORMAT_COLLECTION_LENGTH));
	const boundedDefinitions = Schema.Record(BoundedSchemaProperty, ResponseFormatNode).check(Schema.isMaxProperties(MAX_RESPONSE_FORMAT_COLLECTION_LENGTH));
	const boundedRequired = Schema.Array(BoundedSchemaProperty).check(Schema.isMaxLength(MAX_RESPONSE_FORMAT_COLLECTION_LENGTH), Schema.isUnique());
	const subschema = Schema.Union([Schema.Boolean, ResponseFormatNode]);
	return {
		$schema: Schema.optionalKey(BoundedSchemaReference),
		$id: Schema.optionalKey(BoundedSchemaReference),
		$ref: Schema.optionalKey(BoundedSchemaReference),
		$anchor: Schema.optionalKey(BoundedSchemaReference),
		$dynamicRef: Schema.optionalKey(BoundedSchemaReference),
		$dynamicAnchor: Schema.optionalKey(BoundedSchemaReference),
		$comment: Schema.optionalKey(BoundedSchemaText),
		title: Schema.optionalKey(BoundedSchemaText),
		description: Schema.optionalKey(BoundedSchemaText),
		default: Schema.optionalKey(Schema.Json),
		deprecated: Schema.optionalKey(Schema.Boolean),
		readOnly: Schema.optionalKey(Schema.Boolean),
		writeOnly: Schema.optionalKey(Schema.Boolean),
		examples: Schema.optionalKey(Schema.Array(Schema.Json).check(Schema.isMaxLength(MAX_RESPONSE_FORMAT_COLLECTION_LENGTH))),
		type: Schema.optionalKey(Schema.Union([ResponseFormatPrimitive, ResponseFormatPrimitiveList])),
		enum: Schema.optionalKey(Schema.Array(Schema.Json).check(Schema.isMinLength(1), Schema.isMaxLength(MAX_RESPONSE_FORMAT_COLLECTION_LENGTH), Schema.isUnique())),
		const: Schema.optionalKey(Schema.Json),
		multipleOf: Schema.optionalKey(Schema.Finite.check(Schema.isGreaterThan(0))),
		maximum: Schema.optionalKey(Schema.Finite),
		exclusiveMaximum: Schema.optionalKey(Schema.Finite),
		minimum: Schema.optionalKey(Schema.Finite),
		exclusiveMinimum: Schema.optionalKey(Schema.Finite),
		maxLength: Schema.optionalKey(Schema.Natural),
		minLength: Schema.optionalKey(Schema.Natural),
		pattern: Schema.optionalKey(BoundedSchemaText),
		format: Schema.optionalKey(BoundedSchemaText),
		maxItems: Schema.optionalKey(Schema.Natural),
		minItems: Schema.optionalKey(Schema.Natural),
		uniqueItems: Schema.optionalKey(Schema.Boolean),
		prefixItems: Schema.optionalKey(boundedNodes),
		items: Schema.optionalKey(subschema),
		additionalItems: Schema.optionalKey(subschema),
		unevaluatedItems: Schema.optionalKey(subschema),
		contains: Schema.optionalKey(subschema),
		minContains: Schema.optionalKey(Schema.Natural),
		maxContains: Schema.optionalKey(Schema.Natural),
		properties: Schema.optionalKey(boundedDefinitions),
		patternProperties: Schema.optionalKey(boundedDefinitions),
		required: Schema.optionalKey(boundedRequired),
		dependentRequired: Schema.optionalKey(Schema.Record(BoundedSchemaProperty, boundedRequired).check(Schema.isMaxProperties(MAX_RESPONSE_FORMAT_COLLECTION_LENGTH))),
		dependentSchemas: Schema.optionalKey(boundedDefinitions),
		additionalProperties: Schema.optionalKey(subschema),
		unevaluatedProperties: Schema.optionalKey(subschema),
		propertyNames: Schema.optionalKey(subschema),
		minProperties: Schema.optionalKey(Schema.Natural),
		maxProperties: Schema.optionalKey(Schema.Natural),
		anyOf: Schema.optionalKey(boundedNodes),
		oneOf: Schema.optionalKey(boundedNodes),
		allOf: Schema.optionalKey(boundedNodes),
		not: Schema.optionalKey(subschema),
		contentEncoding: Schema.optionalKey(BoundedSchemaText),
		contentMediaType: Schema.optionalKey(BoundedSchemaText),
		contentSchema: Schema.optionalKey(subschema),
		$defs: Schema.optionalKey(boundedDefinitions),
		definitions: Schema.optionalKey(boundedDefinitions)
	};
};
const ResponseFormatDocument = Schema.Struct({
	...responseFormatFields(),
	type: Schema.Literal("object")
}).pipe(Schema.annotate({ parseOptions: { onExcessProperty: "error" } }));
const isResponseFormatDocument = Schema.is(ResponseFormatDocument);
/** Preflight graph bounds before recursively decoding an untrusted JSON Schema. */
const isBoundedResponseFormat = (input) => {
	if (!Predicate.isObject(input)) return false;
	const pending = [{
		value: input,
		depth: 0
	}];
	const visited = /* @__PURE__ */ new WeakSet();
	let nodes = 0;
	let textUnits = 0;
	try {
		while (pending.length > 0) {
			const current = pending.pop();
			if (current === void 0 || current.depth > MAX_RESPONSE_FORMAT_DEPTH || ++nodes > MAX_RESPONSE_FORMAT_NODES) return false;
			const value = current.value;
			if (value === null || typeof value === "boolean") continue;
			if (typeof value === "number") {
				if (!Number.isFinite(value)) return false;
				continue;
			}
			if (typeof value === "string") {
				textUnits += value.length;
				if (textUnits > MAX_RESPONSE_FORMAT_BYTES) return false;
				continue;
			}
			if (!Predicate.isObjectOrArray(value) || visited.has(value)) return false;
			visited.add(value);
			const entries = Array.isArray(value) ? value.map((entry, index) => [index, entry]) : Object.entries(value);
			if (entries.length > MAX_RESPONSE_FORMAT_COLLECTION_LENGTH) return false;
			for (const [key, entry] of entries) {
				textUnits += typeof key === "string" ? key.length : 0;
				if (textUnits > MAX_RESPONSE_FORMAT_BYTES) return false;
				pending.push({
					value: entry,
					depth: current.depth + 1
				});
			}
		}
		if (!isResponseFormatDocument(input)) return false;
		const encoded = JSON.stringify(input);
		return Encoding.encodeHex(encoded).length / 2 <= MAX_RESPONSE_FORMAT_BYTES;
	} catch {
		return false;
	}
};
/** A platform-supported object JSON Schema with bounded depth, entries, nodes, and UTF-8 bytes. */
const PageCaptureResponseFormat = Schema.declare(isBoundedResponseFormat, {
	identifier: "@effect-agent/sandbox/PageCaptureResponseFormat",
	description: "An object JSON Schema bounded to 64 KiB, depth 32, and 4096 nodes"
});
/** Capture a URL after a full render, the common case. */
var PageUrlTarget = class extends Schema.TaggedClass()("PageUrlTarget", { url: PageCaptureTargetUrl }) {};
/** Render caller-supplied HTML instead of navigating. */
var PageHtmlTarget = class extends Schema.TaggedClass()("PageHtmlTarget", { html: BoundedHtml }) {};
/** What the pass renders: a navigated URL or raw HTML. */
const PageCaptureTarget = Schema.Union([PageUrlTarget, PageHtmlTarget]);
/**
* The rendering engine. `chromium` is the full headless browser; `kitesurf`
* is a lightweight stateless engine an adapter may support for lower-cost
* captures. An adapter that cannot honor the requested engine
* rejects it typed rather than silently substituting the other.
*/
const PageCaptureEngine = Schema.Literals(["chromium", "kitesurf"]);
/** Return the fully rendered HTML document. */
var CapturePageContent = class extends Schema.TaggedClass()("CapturePageContent", {}) {};
/** Return the rendered page converted to Markdown. */
var CapturePageMarkdown = class extends Schema.TaggedClass()("CapturePageMarkdown", {}) {};
/** Return the hyperlinks discovered on the rendered page. */
var CapturePageLinks = class extends Schema.TaggedClass()("CapturePageLinks", { visibleLinksOnly: Schema.optionalKey(Schema.Boolean) }) {};
/** Return rendered element records grouped by their requested CSS selector. */
var CapturePageScrape = class extends Schema.TaggedClass()("CapturePageScrape", { selectors: Schema.Array(BoundedSelector).check(Schema.isMinLength(1), Schema.isMaxLength(MAX_SCRAPE_SELECTORS)) }) {};
/**
* Extract structured data from the rendered page. `responseFormat` is a
* bounded, object-shaped JSON Schema document, typically derived from an
* Effect Schema. The untrusted result must still be decoded by the caller
* through the original Effect Schema.
*/
var CapturePageStructured = class extends Schema.TaggedClass()("CapturePageStructured", {
	responseFormat: PageCaptureResponseFormat,
	prompt: Schema.optionalKey(BoundedPrompt)
}) {};
/** Exactly one bounded output per pass. */
const PageCaptureAction = Schema.Union([
	CapturePageContent,
	CapturePageMarkdown,
	CapturePageLinks,
	CapturePageScrape,
	CapturePageStructured
]);
/** Wait for one CSS selector to appear before capturing. */
var PageSelectorWait = class extends Schema.Class("PageSelectorWait")({
	selector: BoundedSelector,
	timeoutMillis: Schema.optionalKey(BoundedTimeoutMillis)
}) {};
/** Navigation and readiness options applied before the capture. */
var PageNavigationOptions = class extends Schema.Class("PageNavigationOptions")({
	waitUntil: Schema.optionalKey(Schema.Literals([
		"load",
		"domcontentloaded",
		"networkidle0",
		"networkidle2"
	])),
	timeoutMillis: Schema.optionalKey(BoundedTimeoutMillis),
	waitForSelector: Schema.optionalKey(PageSelectorWait)
}) {};
/** The rendered viewport. */
var PageViewport = class extends Schema.Class("PageViewport")({
	width: PositiveInt.check(Schema.isLessThanOrEqualTo(7680)),
	height: PositiveInt.check(Schema.isLessThanOrEqualTo(7680))
}) {};
/**
* Request egress policy for the render. Rejected resource types are never
* fetched; when request patterns are given, only matching navigation,
* redirect, and subresource requests are allowed. Calling capabilities own
* the capture-target allowlist and project it into this policy so rendered
* JavaScript cannot escape the same construction-time authority.
*/
var PageResourcePolicy = class extends Schema.Class("PageResourcePolicy")({
	rejectResourceTypes: Schema.optionalKey(Schema.Array(Schema.Literals([
		"document",
		"stylesheet",
		"image",
		"media",
		"font",
		"script",
		"texttrack",
		"xhr",
		"fetch",
		"eventsource",
		"websocket",
		"manifest",
		"other"
	])).check(Schema.isMaxLength(16))),
	allowRequestPatterns: Schema.optionalKey(Schema.Array(BoundedPattern).check(Schema.isMaxLength(64)))
}) {};
/** Limits an adapter must either enforce or reject. Bytes are UTF-8 encoded. */
var PageCaptureLimits = class extends Schema.Class("PageCaptureLimits")({ maxOutputBytes: PositiveInt.check(Schema.isLessThanOrEqualTo(MAX_OUTPUT_BYTES)) }) {};
/** Schema-first page capture request: one target, one action, one output. */
var PageCaptureRequest = class extends Schema.Class("PageCaptureRequest")({
	target: PageCaptureTarget,
	action: PageCaptureAction,
	engine: PageCaptureEngine,
	limits: PageCaptureLimits,
	navigation: Schema.optionalKey(PageNavigationOptions),
	viewport: Schema.optionalKey(PageViewport),
	resourcePolicy: Schema.optionalKey(PageResourcePolicy)
}) {};
/** The rendered HTML document. */
var PageContentCaptured = class extends Schema.TaggedClass()("PageContentCaptured", { html: BoundedOutputText }) {};
/** The rendered page as Markdown. */
var PageMarkdownCaptured = class extends Schema.TaggedClass()("PageMarkdownCaptured", { markdown: BoundedOutputText }) {};
/** The hyperlinks discovered on the rendered page. */
var PageLinksCaptured = class extends Schema.TaggedClass()("PageLinksCaptured", { links: Schema.Array(PageCaptureLinkUrl).check(Schema.isMaxLength(MAX_LINKS)) }) {};
/** The extracted structured value; untrusted until decoded by the caller. */
var PageStructuredCaptured = class extends Schema.TaggedClass()("PageStructuredCaptured", { value: Schema.Json }) {};
/** One bounded HTML attribute returned by a selector scrape. */
var PageScrapeAttribute = class extends Schema.Class("PageScrapeAttribute")({
	name: BoundedAttributeName,
	value: BoundedAttributeValue
}) {};
/** One rendered element returned by a selector scrape. */
var PageScrapeElement = class extends Schema.Class("PageScrapeElement")({
	text: BoundedScrapeText,
	html: BoundedScrapeHtml,
	attributes: Schema.Array(PageScrapeAttribute).check(Schema.isMaxLength(MAX_SCRAPE_ATTRIBUTES)),
	left: Schema.Finite,
	top: Schema.Finite,
	width: Schema.Finite,
	height: Schema.Finite
}) {};
/** All rendered elements matched by one requested selector. */
var PageScrapeGroup = class extends Schema.Class("PageScrapeGroup")({
	selector: BoundedSelector,
	results: Schema.Array(PageScrapeElement).check(Schema.isMaxLength(MAX_SCRAPE_ELEMENTS))
}) {};
const PageScrapeGroups = Schema.Array(PageScrapeGroup).check(Schema.isMaxLength(MAX_SCRAPE_SELECTORS), Schema.makeFilter((groups) => groups.reduce((total, group) => total + group.results.length, 0) <= MAX_SCRAPE_ELEMENTS, { title: "at most 4096 aggregate scraped elements" }));
/** Bounded rendered element records, preserving selector grouping. */
var PageScrapeCaptured = class extends Schema.TaggedClass()("PageScrapeCaptured", { groups: PageScrapeGroups }) {};
/** Exactly one output kind per pass, matching the requested action. */
const PageCaptureOutput = Schema.Union([
	PageContentCaptured,
	PageMarkdownCaptured,
	PageLinksCaptured,
	PageScrapeCaptured,
	PageStructuredCaptured
]);
/** Explicit accounting for model inference performed by a capture adapter. */
var PageCaptureInferenceUse = class extends Schema.Class("PageCaptureInferenceUse")({
	provider: BoundedInferenceProvider,
	modelCalls: PositiveInt
}) {};
/** Accounting an adapter observed for one capture. */
var PageCaptureResourceUse = class extends Schema.Class("PageCaptureResourceUse")({
	browserMillis: Schema.optionalKey(Schema.Natural),
	inference: Schema.optionalKey(PageCaptureInferenceUse)
}) {};
/** The bounded outcome of one capture pass. */
var PageCaptureResult = class extends Schema.Class("PageCaptureResult")({
	implementation: SandboxImplementation,
	output: PageCaptureOutput,
	resourceUse: PageCaptureResourceUse
}) {};
/**
* The platform refused the pass for rate or quota reasons. `retryAfterMillis`
* carries the platform's own backoff hint when one exists; retrying earlier
* only re-fails.
*/
var PageCaptureRateLimitedError = class extends Schema.TaggedError()("PageCaptureRateLimitedError", {
	implementation: SandboxImplementation,
	reason: Schema.Literals(["rate", "quota"]),
	retryAfterMillis: Schema.optionalKey(Schema.Natural),
	message: BoundedMessage,
	cause: Schema.optionalKey(Schema.Defect())
}) {};
/** Navigation, readiness, or capture of the target page failed. */
var PageCaptureNavigationError = class extends Schema.TaggedError()("PageCaptureNavigationError", {
	implementation: SandboxImplementation,
	message: BoundedMessage,
	cause: Schema.optionalKey(Schema.Defect())
}) {};
/** Host authorization or accounting blocked provider inference before capture. */
var PageCaptureInferencePolicyError = class extends Schema.TaggedError()("PageCaptureInferencePolicyError", {
	implementation: SandboxImplementation,
	provider: BoundedInferenceProvider,
	reason: Schema.Literals(["authorization", "accounting"]),
	message: BoundedMessage,
	cause: Schema.optionalKey(Schema.Defect())
}) {};
/** The adapter cannot honestly honor one requested feature (CAP-010 idiom). */
var PageCaptureUnsupportedError = class extends Schema.TaggedError()("PageCaptureUnsupportedError", {
	implementation: SandboxImplementation,
	feature: Schema.Literals([
		"engine",
		"action",
		"target",
		"navigation",
		"viewport",
		"resource-policy"
	]),
	message: BoundedMessage
}) {};
/** The response exceeded the request's output byte budget. */
var PageCaptureOutputLimitError = class extends Schema.TaggedError()("PageCaptureOutputLimitError", {
	implementation: SandboxImplementation,
	limit: PositiveInt,
	observed: Schema.Natural
}) {};
/** The platform or transport produced a value outside the capture protocol. */
var PageCaptureProtocolError = class extends Schema.TaggedError()("PageCaptureProtocolError", {
	implementation: SandboxImplementation,
	message: BoundedMessage,
	cause: Schema.optionalKey(Schema.Defect())
}) {};
/** Expected capture failures. Interruption stays an Effect interruption. */
const PageCaptureError = Schema.Union([
	PageCaptureRateLimitedError,
	PageCaptureNavigationError,
	PageCaptureInferencePolicyError,
	PageCaptureUnsupportedError,
	PageCaptureOutputLimitError,
	PageCaptureProtocolError
]);
/**
* Stateless page-capture port. One `capture` is one pass: the adapter owns no
* session, replay, approval, or Thread semantics, identifies its
* isolation posture honestly (CAP-010), enforces or rejects every requested
* limit and policy, and returns exactly one bounded output. Capture results
* are untrusted input to whatever reads them.
*/
var PageCapture = class extends Context.Service()("@effect-agent/sandbox/PageCapture") {};
//#endregion
export { CapturePageContent, CapturePageLinks, CapturePageMarkdown, CapturePageScrape, CapturePageStructured, PageCapture, PageCaptureAction, PageCaptureEngine, PageCaptureError, PageCaptureInferencePolicyError, PageCaptureInferenceUse, PageCaptureLimits, PageCaptureLinkUrl, PageCaptureNavigationError, PageCaptureOutput, PageCaptureOutputLimitError, PageCaptureProtocolError, PageCaptureRateLimitedError, PageCaptureRequest, PageCaptureResourceUse, PageCaptureResponseFormat, PageCaptureResult, PageCaptureTarget, PageCaptureTargetUrl, PageCaptureUnsupportedError, PageContentCaptured, PageHtmlTarget, PageLinksCaptured, PageMarkdownCaptured, PageNavigationOptions, PageResourcePolicy, PageScrapeAttribute, PageScrapeCaptured, PageScrapeElement, PageScrapeGroup, PageSelectorWait, PageStructuredCaptured, PageUrlTarget, PageViewport, PageCapture_exports as t };

//# sourceMappingURL=PageCapture.mjs.map