import { t as __exportAll } from "./rolldown-runtime-D7D4PA-g.mjs";
import { SandboxImplementation } from "./Sandbox.mjs";
import { PageCaptureEngine, PageCaptureNavigationError, PageCaptureProtocolError, PageCaptureRateLimitedError, PageCaptureTarget, PageCaptureUnsupportedError, PageNavigationOptions, PageResourcePolicy, PageViewport } from "./PageCapture.mjs";
import { Context, Schema } from "effect";
//#region src/PageScreenshot.ts
var PageScreenshot_exports = /* @__PURE__ */ __exportAll({
	PageScreenshot: () => PageScreenshot,
	PageScreenshotError: () => PageScreenshotError,
	PageScreenshotLimits: () => PageScreenshotLimits,
	PageScreenshotOutputLimitError: () => PageScreenshotOutputLimitError,
	PageScreenshotRequest: () => PageScreenshotRequest,
	PageScreenshotResult: () => PageScreenshotResult
});
const MAX_SCREENSHOT_BYTES = 8388608;
const PositiveInt = Schema.Int.check(Schema.isGreaterThan(0));
/** The byte limit and full-page choice are fixed before Browser Run starts. */
var PageScreenshotLimits = class extends Schema.Class("PageScreenshotLimits")({ maxOutputBytes: PositiveInt.check(Schema.isLessThanOrEqualTo(MAX_SCREENSHOT_BYTES)) }) {};
/** Schema-first request for exactly one PNG screenshot. */
var PageScreenshotRequest = class extends Schema.Class("PageScreenshotRequest")({
	target: PageCaptureTarget,
	engine: PageCaptureEngine,
	limits: PageScreenshotLimits,
	fullPage: Schema.Boolean,
	navigation: Schema.optionalKey(PageNavigationOptions),
	viewport: Schema.optionalKey(PageViewport),
	resourcePolicy: Schema.optionalKey(PageResourcePolicy)
}) {};
/** The only successful output: bytes are caller-owned and never durable framework data. */
var PageScreenshotResult = class extends Schema.Class("PageScreenshotResult")({
	implementation: SandboxImplementation,
	mediaType: Schema.Literal("image/png"),
	bytes: Schema.Uint8Array.check(Schema.isMaxLength(MAX_SCREENSHOT_BYTES))
}) {};
/** The response crossed the request's PNG byte limit. */
var PageScreenshotOutputLimitError = class extends Schema.TaggedError()("PageScreenshotOutputLimitError", {
	implementation: SandboxImplementation,
	limit: PositiveInt,
	observed: Schema.Natural
}) {};
/** Expected screenshot failures. The shared capture errors keep identical semantics. */
const PageScreenshotError = Schema.Union([
	PageCaptureRateLimitedError,
	PageCaptureNavigationError,
	PageCaptureUnsupportedError,
	PageCaptureProtocolError,
	PageScreenshotOutputLimitError
]);
/** Stateless, one-output PNG capture port. It owns neither persistence nor later byte handoff. */
var PageScreenshot = class extends Context.Service()("@effect-agent/sandbox/PageScreenshot") {};
//#endregion
export { PageScreenshot, PageScreenshotError, PageScreenshotLimits, PageScreenshotOutputLimitError, PageScreenshotRequest, PageScreenshotResult, PageScreenshot_exports as t };

//# sourceMappingURL=PageScreenshot.mjs.map