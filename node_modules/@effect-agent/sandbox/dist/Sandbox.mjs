import { t as __exportAll } from "./rolldown-runtime-D7D4PA-g.mjs";
import { Context, Duration, Schema } from "effect";
//#region src/Sandbox.ts
var Sandbox_exports = /* @__PURE__ */ __exportAll({
	NetworkAllowlist: () => NetworkAllowlist,
	NetworkDisabled: () => NetworkDisabled,
	SANDBOX_DIAGNOSTIC_MAX_LENGTH: () => SANDBOX_DIAGNOSTIC_MAX_LENGTH,
	Sandbox: () => Sandbox,
	SandboxArtifact: () => SandboxArtifact,
	SandboxArtifactRule: () => SandboxArtifactRule,
	SandboxEnvironment: () => SandboxEnvironment,
	SandboxError: () => SandboxError,
	SandboxEvent: () => SandboxEvent,
	SandboxExitError: () => SandboxExitError,
	SandboxExited: () => SandboxExited,
	SandboxImplementation: () => SandboxImplementation,
	SandboxLimits: () => SandboxLimits,
	SandboxMount: () => SandboxMount,
	SandboxNetworkPolicy: () => SandboxNetworkPolicy,
	SandboxOutput: () => SandboxOutput,
	SandboxOutputLimitError: () => SandboxOutputLimitError,
	SandboxRequest: () => SandboxRequest,
	SandboxResourceUse: () => SandboxResourceUse,
	SandboxRuntime: () => SandboxRuntime,
	SandboxSecretHandle: () => SandboxSecretHandle,
	SandboxSpawnError: () => SandboxSpawnError,
	SandboxStarted: () => SandboxStarted,
	SandboxTimeoutError: () => SandboxTimeoutError,
	SandboxUnsupportedRequestError: () => SandboxUnsupportedRequestError
});
const BoundedName = Schema.NonEmptyString.check(Schema.isMaxLength(256));
const BoundedPath = Schema.NonEmptyString.check(Schema.isMaxLength(4096));
const BoundedArgument = Schema.String.check(Schema.isMaxLength(32768));
const MAX_OUTPUT_BYTES = 16777216;
const MAX_ARTIFACT_RULES = 64;
const BoundedOutputText = Schema.String.check(Schema.isMaxLength(MAX_OUTPUT_BYTES));
const SANDBOX_DIAGNOSTIC_MAX_LENGTH = 8192;
const BoundedDiagnostic = Schema.String.check(Schema.isMaxLength(SANDBOX_DIAGNOSTIC_MAX_LENGTH));
const PositiveInt = Schema.Int.check(Schema.isGreaterThan(0));
const PositiveNumber = Schema.Finite.check(Schema.isGreaterThan(0));
const MaxOutputBytes = PositiveInt.check(Schema.isLessThanOrEqualTo(MAX_OUTPUT_BYTES));
const BoundedArguments = Schema.Array(BoundedArgument).check(Schema.isMaxLength(256));
const BoundedEnvironmentNames = Schema.Array(BoundedName).check(Schema.isMaxLength(128));
const FinitePositiveDuration = Schema.Duration.pipe(Schema.refine((duration) => Duration.isFinite(duration) && Duration.isPositive(duration), { expected: "a finite positive duration" }));
const FiniteNonNegativeDuration = Schema.Duration.pipe(Schema.refine((duration) => Duration.isFinite(duration) && !Duration.isNegative(duration), { expected: "a finite non-negative duration" }));
/** A requested runtime image or executable environment. */
var SandboxRuntime = class extends Schema.Class("SandboxRuntime")({
	kind: Schema.Literals([
		"container",
		"microvm",
		"wasm",
		"unisolated-process"
	]),
	identity: BoundedName
}) {};
/** The implementation and its isolation posture that executed a request. */
var SandboxImplementation = class extends Schema.Class("SandboxImplementation")({
	isolation: Schema.Literals(["isolated", "unisolated"]),
	identity: BoundedName
}) {};
/** A host path made available to the requested runtime. */
var SandboxMount = class extends Schema.Class("SandboxMount")({
	source: BoundedPath,
	target: BoundedPath,
	access: Schema.Literals(["read-only", "read-write"])
}) {};
/** Explicitly denies all workload network access. */
var NetworkDisabled = class extends Schema.TaggedClass()("NetworkDisabled", {}) {};
/** Explicitly permits connections only to the named destination and port allowlists. */
var NetworkAllowlist = class extends Schema.TaggedClass()("NetworkAllowlist", {
	domains: Schema.Array(BoundedName).check(Schema.isMaxLength(256)),
	ports: Schema.Array(PositiveInt.check(Schema.isLessThanOrEqualTo(65535))).check(Schema.isMaxLength(256))
}) {};
/** Network access policy supplied to a sandbox implementation. */
const SandboxNetworkPolicy = Schema.Union([NetworkDisabled, NetworkAllowlist]);
/** Names of ambient variables that may be copied into the workload environment. */
var SandboxEnvironment = class extends Schema.Class("SandboxEnvironment")({ allow: BoundedEnvironmentNames }) {};
/** Limits that a sandbox implementation must either enforce or reject. */
var SandboxLimits = class extends Schema.Class("SandboxLimits")({
	cpuCores: Schema.optionalKey(PositiveNumber.check(Schema.isLessThanOrEqualTo(1024))),
	memoryBytes: Schema.optionalKey(PositiveInt.check(Schema.isLessThanOrEqualTo(1099511627776))),
	maxOutputBytes: MaxOutputBytes,
	maxWallTime: FinitePositiveDuration
}) {};
/** A secret reference; raw secret values are never part of a sandbox request. */
var SandboxSecretHandle = class extends Schema.Class("SandboxSecretHandle")({
	id: BoundedName,
	purpose: Schema.NonEmptyString.check(Schema.isMaxLength(2048))
}) {};
/** Rules for copying selected workload output out of an isolated runtime. */
var SandboxArtifactRule = class extends Schema.Class("SandboxArtifactRule")({
	path: BoundedPath,
	maxBytes: MaxOutputBytes
}) {};
/** Metadata for an artifact that a sandbox implementation released after applying its rules. */
var SandboxArtifact = class extends Schema.Class("SandboxArtifact")({
	path: BoundedPath,
	bytes: Schema.Natural.check(Schema.isLessThanOrEqualTo(MAX_OUTPUT_BYTES)),
	digest: BoundedName,
	mediaType: Schema.optionalKey(BoundedName)
}) {};
/** Schema-first command execution request. Command and arguments are intentionally separate. */
var SandboxRequest = class extends Schema.Class("SandboxRequest")({
	runtime: SandboxRuntime,
	command: BoundedPath,
	args: BoundedArguments,
	cwd: BoundedPath,
	environment: SandboxEnvironment,
	mounts: Schema.Array(SandboxMount).check(Schema.isMaxLength(64)),
	network: SandboxNetworkPolicy,
	limits: SandboxLimits,
	secretHandles: Schema.Array(SandboxSecretHandle).check(Schema.isMaxLength(64)),
	artifactRules: Schema.Array(SandboxArtifactRule).check(Schema.isMaxLength(MAX_ARTIFACT_RULES))
}) {};
/** Accounting that a sandbox implementation was able to observe for one execution. */
var SandboxResourceUse = class extends Schema.Class("SandboxResourceUse")({
	wallTime: FiniteNonNegativeDuration,
	stdoutBytes: Schema.Natural.check(Schema.isLessThanOrEqualTo(MAX_OUTPUT_BYTES)),
	stderrBytes: Schema.Natural.check(Schema.isLessThanOrEqualTo(MAX_OUTPUT_BYTES)),
	cpuMillis: Schema.optionalKey(Schema.Natural),
	memoryBytes: Schema.optionalKey(Schema.Natural)
}) {};
const SandboxEventBase = {
	eventVersion: Schema.Literal(1),
	implementation: SandboxImplementation
};
/** Signals that the implementation has accepted the request and started its workload. */
var SandboxStarted = class extends Schema.TaggedClass()("SandboxStarted", {
	...SandboxEventBase,
	runtime: SandboxRuntime
}) {};
/** A bounded text fragment from stdout or stderr. Sandbox output is sensitive by default. */
var SandboxOutput = class extends Schema.TaggedClass()("SandboxOutput", {
	...SandboxEventBase,
	stream: Schema.Literals(["stdout", "stderr"]),
	text: BoundedOutputText,
	bytes: Schema.Natural.check(Schema.isLessThanOrEqualTo(MAX_OUTPUT_BYTES))
}) {};
/** The terminal process status and observed bounded resource usage. */
var SandboxExited = class extends Schema.TaggedClass()("SandboxExited", {
	...SandboxEventBase,
	exitCode: Schema.Int,
	resourceUse: SandboxResourceUse,
	artifacts: Schema.Array(SandboxArtifact).check(Schema.isMaxLength(MAX_ARTIFACT_RULES))
}) {};
/** Versioned execution event stream. */
const SandboxEvent = Schema.Union([
	SandboxStarted,
	SandboxOutput,
	SandboxExited
]);
/** The runtime could not start the requested process. */
var SandboxSpawnError = class extends Schema.TaggedError()("SandboxSpawnError", {
	implementation: SandboxImplementation,
	command: BoundedPath,
	message: BoundedDiagnostic,
	cause: Schema.optionalKey(Schema.Defect())
}) {};
/** The process ended unsuccessfully after starting. */
var SandboxExitError = class extends Schema.TaggedError()("SandboxExitError", {
	implementation: SandboxImplementation,
	exitCode: Schema.Int,
	message: BoundedDiagnostic,
	cause: Schema.optionalKey(Schema.Defect())
}) {};
/** The wall-clock execution limit elapsed and the owned process was interrupted. */
var SandboxTimeoutError = class extends Schema.TaggedError()("SandboxTimeoutError", {
	implementation: SandboxImplementation,
	maxWallTime: FinitePositiveDuration
}) {};
/** A stdout or stderr stream exceeded its configured bounded-output limit. */
var SandboxOutputLimitError = class extends Schema.TaggedError()("SandboxOutputLimitError", {
	implementation: SandboxImplementation,
	stream: Schema.Literals(["stdout", "stderr"]),
	limit: PositiveInt,
	observed: PositiveInt
}) {};
/** The selected implementation cannot honestly enforce one requested security or resource feature. */
var SandboxUnsupportedRequestError = class extends Schema.TaggedError()("SandboxUnsupportedRequestError", {
	implementation: SandboxImplementation,
	feature: Schema.Literals([
		"runtime",
		"mounts",
		"network",
		"cpu-limit",
		"memory-limit",
		"secret-handles",
		"artifacts"
	]),
	message: BoundedDiagnostic
}) {};
/** Expected sandbox execution failures. Interruption remains an Effect interruption, never a fabricated exit. */
const SandboxError = Schema.Union([
	SandboxSpawnError,
	SandboxExitError,
	SandboxTimeoutError,
	SandboxOutputLimitError,
	SandboxUnsupportedRequestError
]);
/**
* Scoped command/code execution capability. Implementations must identify their isolation posture
* in every emitted event; an `unisolated` implementation is never a security sandbox.
*/
var Sandbox = class extends Context.Service()("@effect-agent/sandbox/Sandbox") {};
//#endregion
export { NetworkAllowlist, NetworkDisabled, SANDBOX_DIAGNOSTIC_MAX_LENGTH, Sandbox, SandboxArtifact, SandboxArtifactRule, SandboxEnvironment, SandboxError, SandboxEvent, SandboxExitError, SandboxExited, SandboxImplementation, SandboxLimits, SandboxMount, SandboxNetworkPolicy, SandboxOutput, SandboxOutputLimitError, SandboxRequest, SandboxResourceUse, SandboxRuntime, SandboxSecretHandle, SandboxSpawnError, SandboxStarted, SandboxTimeoutError, SandboxUnsupportedRequestError, Sandbox_exports as t };

//# sourceMappingURL=Sandbox.mjs.map