import { t as __exportAll } from "./rolldown-runtime-D7D4PA-g.mjs";
import { InteractiveBrowserHost } from "./InteractiveBrowser.mjs";
import { Context, Effect, Layer, Schema, Scope } from "effect";
//#region src/ProtectedBrowser.ts
var ProtectedBrowser_exports = /* @__PURE__ */ __exportAll({
	BrowserCredentialAccess: () => BrowserCredentialAccess,
	BrowserCredentialMaterial: () => BrowserCredentialMaterial,
	BrowserReference: () => BrowserReference,
	CardCredential: () => CardCredential,
	CredentialAccessError: () => CredentialAccessError,
	CredentialDispatch: () => CredentialDispatch,
	CredentialFieldRole: () => CredentialFieldRole,
	CredentialKind: () => CredentialKind,
	CredentialMilestone: () => CredentialMilestone,
	CredentialOffer: () => CredentialOffer,
	CredentialOfferMetadata: () => CredentialOfferMetadata,
	CredentialOrigin: () => CredentialOrigin,
	CredentialTarget: () => CredentialTarget,
	CredentialUseResult: () => CredentialUseResult,
	ListCredentialOffers: () => ListCredentialOffers,
	LoginCredential: () => LoginCredential,
	ProtectedBrowser: () => ProtectedBrowser,
	ProtectedBrowserClick: () => ProtectedBrowserClick,
	ProtectedBrowserControl: () => ProtectedBrowserControl,
	ProtectedBrowserError: () => ProtectedBrowserError,
	ProtectedBrowserNavigate: () => ProtectedBrowserNavigate,
	ProtectedBrowserObservation: () => ProtectedBrowserObservation,
	ProtectedBrowserSession: () => ProtectedBrowserSession,
	ProtectedCleanup: () => ProtectedCleanup,
	ProtectedObservationState: () => ProtectedObservationState,
	UseCredential: () => UseCredential
});
/** Canonical HTTPS origin, including a non-default port. Host grants match exactly. */
const CredentialOrigin = Schema.String.check(Schema.makeFilter((value) => value.startsWith("https://") && Schema.is(InteractiveBrowserHost)(value.slice(8))));
const BrowserReference = Schema.String.check(Schema.isUUID());
const CredentialKind = Schema.Literals(["login", "card"]);
const CredentialFieldRole = Schema.Literals([
	"username",
	"password",
	"card-name",
	"card-number",
	"card-expiry",
	"card-expiry-month",
	"card-expiry-year",
	"card-security-code"
]);
const Label = Schema.String.check(Schema.isMaxLength(200));
var CredentialTarget = class extends Schema.Class("CredentialTarget")({
	topOrigin: CredentialOrigin,
	frameOrigin: CredentialOrigin,
	recipientOrigin: CredentialOrigin,
	document: BrowserReference,
	frame: BrowserReference,
	form: BrowserReference
}) {};
var ProtectedBrowserControl = class extends Schema.Class("ProtectedBrowserControl")({
	ref: BrowserReference,
	target: CredentialTarget,
	role: Schema.Union([CredentialFieldRole, Schema.Literals([
		"submit",
		"link",
		"button",
		"unsupported"
	])]),
	label: Label
}) {};
var ProtectedBrowserObservation = class extends Schema.Class("ProtectedBrowserObservation")({
	document: BrowserReference,
	topOrigin: CredentialOrigin,
	text: Schema.String.check(Schema.isMaxLength(65536)),
	controls: Schema.Array(ProtectedBrowserControl).check(Schema.isMaxLength(64)),
	truncated: Schema.Boolean,
	observation: Schema.Literals(["before-exposure", "approved-after-exposure"])
}) {};
var CredentialOfferMetadata = class extends Schema.Class("CredentialOfferMetadata")({
	label: Label,
	brand: Schema.optionalKey(Label),
	lastFour: Schema.optionalKey(Schema.String.check(Schema.isPattern(/^\d{4}$/)))
}) {};
var CredentialOffer = class extends Schema.Class("CredentialOffer")({
	ref: BrowserReference,
	kind: CredentialKind,
	metadata: CredentialOfferMetadata
}) {};
var ListCredentialOffers = class extends Schema.Class("ListCredentialOffers")({
	kind: CredentialKind,
	target: BrowserReference
}) {};
var UseCredential = class extends Schema.Class("UseCredential")({
	offer: BrowserReference,
	fields: Schema.Array(Schema.Struct({
		ref: BrowserReference,
		role: CredentialFieldRole
	})).check(Schema.isMinLength(1), Schema.isMaxLength(8)),
	submit: Schema.optionalKey(BrowserReference)
}) {};
var ProtectedBrowserNavigate = class extends Schema.Class("ProtectedBrowserNavigate")({ url: Schema.String.check(Schema.isMaxLength(8192)) }) {};
var ProtectedBrowserClick = class extends Schema.Class("ProtectedBrowserClick")({ ref: BrowserReference }) {};
const CredentialDispatch = Schema.Literals([
	"not-dispatched",
	"possibly-dispatched",
	"dispatched"
]);
const CredentialMilestone = Schema.Literals([
	"none",
	"partial-fill",
	"filled",
	"submission-dispatched"
]);
const ProtectedObservationState = Schema.Literals([
	"before-exposure",
	"protected",
	"approved-after-exposure",
	"closed"
]);
const ProtectedCleanup = Schema.Literals([
	"not-requested",
	"confirmed",
	"unconfirmed"
]);
const Evidence = {
	dispatch: CredentialDispatch,
	milestone: CredentialMilestone,
	observation: ProtectedObservationState,
	cleanup: ProtectedCleanup
};
/** No foreign message/cause or page data may enter this failure. Closure does not undo dispatch. */
var ProtectedBrowserError = class extends Schema.TaggedError()("ProtectedBrowserError", {
	reason: Schema.Literals([
		"denied",
		"missing-credential",
		"stale-reference",
		"unsupported",
		"needs-attention",
		"busy",
		"closed",
		"limit",
		"timeout",
		"provider",
		"resolver",
		"outcome-unknown",
		"observation-blocked"
	]),
	...Evidence
}) {};
var CredentialUseResult = class extends Schema.Class("CredentialUseResult")({
	...Evidence,
	authentication: Schema.Literal("unverified")
}) {};
const SecretText = Schema.Redacted(Schema.NonEmptyString.check(Schema.isMaxLength(1024)));
/** Host-only material. Never encode it into Tool results, checkpoints, or storage. */
const LoginCredential = Schema.TaggedStruct("LoginCredential", {
	username: SecretText,
	password: SecretText
});
/** Security codes are transient, never persisted. Dummy tests are not PCI compliance evidence. */
const CardCredential = Schema.TaggedStruct("CardCredential", {
	name: SecretText,
	number: Schema.Redacted(Schema.String.check(Schema.isPattern(/^\d{12,19}$/))),
	expiry: SecretText,
	expiryMonth: Schema.Redacted(Schema.String.check(Schema.isPattern(/^(0[1-9]|1[0-2])$/))),
	expiryYear: Schema.Redacted(Schema.String.check(Schema.isPattern(/^\d{4}$/))),
	securityCode: Schema.optionalKey(Schema.Redacted(Schema.String.check(Schema.isPattern(/^\d{3,4}$/))))
});
const BrowserCredentialMaterial = Schema.Union([LoginCredential, CardCredential]);
var CredentialAccessError = class extends Schema.TaggedError()("CredentialAccessError", { reason: Schema.Literals([
	"denied",
	"missing-credential",
	"needs-attention",
	"resolver"
]) }) {};
/**
* Host-only vault and grant port. Derive caller from the authorized invocation, never Tool input.
* Recheck account ownership, current grants, merchant/frame/recipient pairing, and any side
* effects caused by filling. Listing authorizes metadata only. Grant checks never widen network
* policy. Implementations must not log material or put it in defects/traces.
*/
var BrowserCredentialAccess = class extends Context.Service()("@effect-agent/sandbox/BrowserCredentialAccess") {};
var ProtectedBrowser = class extends Context.Service()("@effect-agent/sandbox/ProtectedBrowser") {};
/** Build once in an execution/Attempt Layer. Acquires lazily and shares one pass across Tools. */
var ProtectedBrowserSession = class extends Context.Service()("@effect-agent/sandbox/ProtectedBrowserSession") {
	static layer(policy) {
		return Layer.effect(this)(Effect.gen(function* () {
			const browser = yield* ProtectedBrowser;
			const access = yield* BrowserCredentialAccess;
			const scope = yield* Effect.scope;
			const cached = yield* Effect.cached(browser.open(policy).pipe(Effect.provideService(BrowserCredentialAccess, access), Effect.provideService(Scope.Scope, scope)));
			return { get: Effect.suspend(() => scope.state._tag === "Closed" ? Effect.fail(new ProtectedBrowserError({
				reason: "closed",
				dispatch: "not-dispatched",
				milestone: "none",
				observation: "closed",
				cleanup: "not-requested"
			})) : cached) };
		}));
	}
};
//#endregion
export { BrowserCredentialAccess, BrowserCredentialMaterial, BrowserReference, CardCredential, CredentialAccessError, CredentialDispatch, CredentialFieldRole, CredentialKind, CredentialMilestone, CredentialOffer, CredentialOfferMetadata, CredentialOrigin, CredentialTarget, CredentialUseResult, ListCredentialOffers, LoginCredential, ProtectedBrowser, ProtectedBrowserClick, ProtectedBrowserControl, ProtectedBrowserError, ProtectedBrowserNavigate, ProtectedBrowserObservation, ProtectedBrowserSession, ProtectedCleanup, ProtectedObservationState, UseCredential, ProtectedBrowser_exports as t };

//# sourceMappingURL=ProtectedBrowser.mjs.map